# MEMORY.md -- 长期经验记忆
#
# 本文件由反思引擎 (ReflectionEngine) 自动维护。
# 记录任务执行中积累的经验、教训和环境知识。
# LLM 规划时会读取此文件，参考历史经验做更好的决策。
#
# 格式: 每条记忆包含时间戳、类别和内容。

## 环境知识

- [2026-03-13 15:23] 当 UAV 状态为 error 时，飞行相关技能（如 takeoff）和基础状态技能（如 get_position）可能会立即失败且无有效执行时间。
- [2026-03-13 15:23] 即便无人机位置和电量数值存在（例如 pos 有高度、电量100%），只要 status=error，就不能假定飞行和感知链路是可用的，需要先进行故障确认或恢复。


- [2026-03-13 15:23] 当 UAV 状态为 error 时，飞行相关技能（如 takeoff）和基础状态技能（如 get_position）可能会立即失败且无有效执行时间。
- [2026-03-13 15:23] 即便无人机位置和电量数值存在（例如 pos 有高度、电量100%），只要 status=error，就不能假定飞行和感知链路是可用的，需要先进行故障确认或恢复。

- [2026-03-13 19:40] 当 UAV 处于 idle 且电量在中高水平时（如电量约大于 50%），基础状态查询技能（如 get_position）可以在极短时间内稳定完成，无需额外的安全检查或预处理。

- [2026-03-13 19:40] 当 UAV 处于 idle 且电量在中高水平时（如电量约大于 50%），基础状态查询技能（如 get_position）可以在极短时间内稳定完成，无需额外的安全检查或预处理。

- [2026-03-13 20:14] 在 status=idle 且高度为负值（例如 pos=[0.44, 0.0, -0.5]）的状态下，land 技能可能会直接失败并显示耗时为 0.0s，推测环境中存在“已经着陆/不在可降落状态”的判定逻辑，此时再次调用 land 不会产生有效动作。
- [2026-03-13 20:14] 当某个飞行控制技能（如 land）连续多次以 0.0s 耗时失败时，很可能意味着当前姿态/状态不满足该技能的前置条件，而不是瞬间的传感器噪声或偶发错误。

- [2026-03-13 20:14] 在 status=idle 且高度为负值（例如 pos=[0.44, 0.0, -0.5]）的状态下，land 技能可能会直接失败并显示耗时为 0.0s，推测环境中存在“已经着陆/不在可降落状态”的判定逻辑，此时再次调用 land 不会产生有效动作。
- [2026-03-13 20:14] 当某个飞行控制技能（如 land）连续多次以 0.0s 耗时失败时，很可能意味着当前姿态/状态不满足该技能的前置条件，而不是瞬间的传感器噪声或偶发错误。

- [2026-03-13 21:14] 在任务主流程全部正常的情况下，返航（return_to_launch）阶段仍可能单独失败并导致 UAV 状态变为 error，说明返航阶段需要被视作高风险阶段进行额外的状态监控与异常处理。
- [2026-03-13 21:14] 在本次任务中，电量仍有较高余量（约69%）时就已出现 return_to_launch 失败，说明返航失败不一定与电量不足直接相关，可能涉及导航、控制或地面条件等因素。

- [2026-03-13 21:14] 在任务主流程全部正常的情况下，返航（return_to_launch）阶段仍可能单独失败并导致 UAV 状态变为 error，说明返航阶段需要被视作高风险阶段进行额外的状态监控与异常处理。
- [2026-03-13 21:14] 在本次任务中，电量仍有较高余量（约69%）时就已出现 return_to_launch 失败，说明返航失败不一定与电量不足直接相关，可能涉及导航、控制或地面条件等因素。

## 任务经验

- [2026-03-28 00:32] 任务: 巡检任务[65,10],[58，76],[-15,50],[-92,82]四个位 [成功] 技能链: takeoff → fly_to → observe → report → fly_to → observe → report → change_altitude → fly_to → observe → report → fly_to → observe → report → fly_to

- [2026-03-28 00:25] 任务: 巡检任务[65,10],[58，76],[-15,50],[-92,82]四个位 [成功] 技能链: takeoff → fly_to → observe → look_around → report → fly_to → observe → look_around → report → fly_to → observe → look_around → report → fly_to → observe → look_around → report → fly_to

- [2026-03-28 00:14] 任务: 巡检任务[65,10],[63，102],[-15,50],[-92,82]四个 [成功] 技能链: takeoff → fly_to → observe → report → fly_to → observe → report → fly_to → observe → report → fly_to → observe → report → fly_to

- [2026-03-27 23:02] 任务: 巡检任务[0,0],[0,-15],[-30,-17],[-33,16]四个位置 [未完成] 技能链: takeoff → fly_to → observe → report → fly_to → observe → report → fly_to → observe → report → fly_to → observe → report → fly_to → observe

- [2026-03-27 22:08] 任务: 巡检任务[-0,0],[0,-15],[-30,-17],[-33,16]四个位 [未完成] 技能链: takeoff → get_position → fly_to → observe → report → fly_to → observe → report → fly_to → observe → report → fly_to → observe → report → fly_to → observe

- [2026-03-27 20:46] 任务: 巡检任务[-0,0],[0,-15],[-30,-17],[-33,16]四个位 [未完成] 技能链: takeoff → fly_to → observe → takeoff → takeoff
  - observe 失败: 前提条件不满足: 
  - takeoff 失败: RPC error: rpclib: function 'takeoff' (called with 2 arg(s)) threw an exception. The exception is not derived from std::exception. No further information available.
  - takeoff 失败: RPC error: rpclib: function 'takeoff' (called with 2 arg(s)) threw an exception. The exception is not derived from std::exception. No further information available.

- [2026-03-27 20:12] 任务: 巡检任务[-0,0],[0,-15],[-30,-17],[-33,16]四个位 [未完成] 技能链: change_altitude → fly_to → hover
  - change_altitude 失败: Skill execution raised exception: 'bool' object is not callable
  - fly_to 失败: Skill execution raised exception: 'bool' object is not callable
  - hover 失败: Skill execution raised exception: 'bool' object is not callable

- [2026-03-27 17:53] 任务: 对周围的建筑区域进行一次巡检，近处的底层建筑,检查有没有异常情况，完成后汇报。 [未完成] 技能链: change_altitude → look_around → fly_to → change_altitude → fly_to → observe → observe → change_altitude → observe → fly_relative → observe → change_altitude → observe → report → observe
  - fly_to 失败: ⚠️ 前方1.8m 处检测到障碍物，已自动悬停。请重新规划航线或改变方向。
  - fly_to 失败: fly_to_ned: timeout

- [2026-03-27 13:56] 任务: 对周围的建筑区域进行一次巡检，近处的底层建筑,检查有没有异常情况，完成后汇报。 [未完成] 技能链: takeoff → fly_to → fly_relative → observe → change_altitude → observe → look_around → observe → observe → observe → fly_relative → change_altitude → observe → report → fly_relative
  - fly_to 失败: fly_to_ned: timeout
  - fly_relative 失败: fly_to_ned: timeout
  - observe 失败: 系统拦截: observe 已连续使用3次且无位置变化, 你必须先移动 (fly_to/fly_relative) 再观察

- [2026-03-27 00:45] 任务: 对周围的建筑区域进行一次巡检，重点关注中层建筑群、近处的底层建筑和高层商业区，检 [未完成] 技能链: change_altitude → change_altitude → fly_to → change_altitude → change_altitude → fly_to → fly_to → look_around → observe → report → fly_to → fly_to → fly_relative → observe
  - fly_to 失败: fly_to_ned: timeout
  - fly_to 失败: fly_to_ned: timeout
  - fly_to 失败: fly_to_ned: timeout
  - fly_relative 失败: fly_to_ned: timeout

- [2026-03-26 23:12] 任务: 对周围的建筑区域进行一次巡检，重点关注中层建筑群、近处的底层建筑和高层商业区，检 [未完成] 技能链: takeoff → change_altitude → look_around → observe → fly_to → observe → report → report → report → fly_to → observe → report → report → report → fly_to
  - look_around 失败: 旋转失败: 'AirSimPhysicsAdapter' object has no attribute 'set_velocity_body'
  - report 失败: 报告内容不能为空
  - report 失败: 报告内容不能为空
  - report 失败: 系统拦截: report 已连续使用3次且无位置变化, 你必须先移动 (fly_to/fly_relative) 再观察
  - report 失败: 报告内容不能为空
  - report 失败: 报告内容不能为空
  - report 失败: 系统拦截: report 已连续使用3次且无位置变化, 你必须先移动 (fly_to/fly_relative) 再观察

- [2026-03-23 21:02] 任务: 起飞到12米，向东飞30米，观察前方，然后返回起飞点降落 [未完成] 技能链: 

- [2026-03-23 20:55] 任务: 起飞到12米，向东飞30米，观察前方，然后返回起飞点降落 [未完成] 技能链: 

- [2026-03-20 20:29] 任务: 起飞到12米，向东飞30米，观察前方，然后返回起飞点降落 [未完成] 技能链: 

- [2026-03-14 22:59] 任务: 起飞到12米，向东飞30米，观察前方，然后返回起飞点降落 [未完成] 技能链: 

- [2026-03-13 20:25] 任务: 起飞到12米，向东飞30米，观察前方，然后返回起飞点降落 [成功] 技能链: takeoff → change_altitude → change_altitude → fly_relative → fly_relative → observe → return_to_launch

- [2026-03-13 15:02] 无人机成功上升至约15米并完成周边环境观察与描述，但观察过程中多次出现 observe 技能失败与最终机体 error 状态。 -- 任务在功能层面达成（成功起飞并获取环境信息），但中途 observe 技能多次失败且结束时 UAV 状态为 error，说明感知链路与状态监控存在不稳定因素；最终成功依赖多轮冗余观察和辅助感知技能（look_around、scan_area）弥补单次 observe 失败。
  - 对于“观察并描述环境”类任务，单次 observe 调用不可靠，应预先规划为多轮观察并结合辅助技能（look_around、scan_area）以提高整体成功率。
  - 当同一感知技能短时间内连续失败时，应在规划层加入退避或切换策略，例如：改用其他感知技能、增加停留时间（hover）、或者调整相机姿态，而不是盲目重复调用。
  - 任务结果判定不能只根据是否获得一次成功观察，还应综合 UAV 最终状态（例如 status=error）；若 UAV 处于错误状态，任务虽标记为成功也要记录为“有风险的成功”，以驱动后续更保守策略。
  - 在当前高度控制精度下，实际高度（14.48m）与目标高度（15m）误差可接受，但规划时应认识到高度存在±1m 级别偏差，避免在严格高度约束场景中依赖单次 change_altitude 即认为高度完全准确。
  - 缺乏历史统计时，首次任务的失败模式（例如 observe 多次失败但最终成功）应被记录为经验，以便后续对同一技能做更保守的期望与参数设定。


- [2026-03-13 15:02] 无人机成功上升至约15米并完成周边环境观察与描述，但观察过程中多次出现 observe 技能失败与最终机体 error 状态。 -- 任务在功能层面达成（成功起飞并获取环境信息），但中途 observe 技能多次失败且结束时 UAV 状态为 error，说明感知链路与状态监控存在不稳定因素；最终成功依赖多轮冗余观察和辅助感知技能（look_around、scan_area）弥补单次 observe 失败。
  - 对于“观察并描述环境”类任务，单次 observe 调用不可靠，应预先规划为多轮观察并结合辅助技能（look_around、scan_area）以提高整体成功率。
  - 当同一感知技能短时间内连续失败时，应在规划层加入退避或切换策略，例如：改用其他感知技能、增加停留时间（hover）、或者调整相机姿态，而不是盲目重复调用。
  - 任务结果判定不能只根据是否获得一次成功观察，还应综合 UAV 最终状态（例如 status=error）；若 UAV 处于错误状态，任务虽标记为成功也要记录为“有风险的成功”，以驱动后续更保守策略。
  - 在当前高度控制精度下，实际高度（14.48m）与目标高度（15m）误差可接受，但规划时应认识到高度存在±1m 级别偏差，避免在严格高度约束场景中依赖单次 change_altitude 即认为高度完全准确。
  - 缺乏历史统计时，首次任务的失败模式（例如 observe 多次失败但最终成功）应被记录为经验，以便后续对同一技能做更保守的期望与参数设定。

- [2026-03-13 15:23] 任务在无人机处于 error 状态下尝试起飞并获取位置信息，导致起飞与定位技能均立即失败，任务整体未开始即告失败。 -- 失败的核心原因是执行任务前无人机已处于 status=error 状态，却仍直接调用 takeoff 和 get_position；在机体处于错误状态时，底层控制/状态查询接口不可用或直接返回失败，导致技能调用在0.0s内即失败，任务没有进入实际飞行与感知阶段。缺乏对初始状态的前置检查和异常恢复逻辑是根本问题。
  - 在规划任何飞行或感知任务前，应首先检查 UAV 状态字段（如 status），若为 error/critical 等异常值，应终止当前任务并尝试调用专门的自检/复位流程，而不是直接调用任务技能。
  - 对于起飞类任务，将前置条件显式化：需要 status 为 ready/idle/ok 且当前高度低于目标高度；否则应优先处理状态异常而不是重复尝试 takeoff。
  - 在技能历史记录为空、且首次调用即 0.0s 失败时，应优先考虑系统状态或接口初始化问题，而不是参数不当，后续规划应插入诊断步骤（查询详细错误、重启控制模块等）。
  - 任务目标是“观察周围环境”时，如果起飞阶段失败，应向上汇报“起飞阶段未完成，因此无法进行观察”，而不是尝试执行后续观察或定位技能，以避免级联失败。

- [2026-03-13 15:23] 任务在无人机处于 error 状态下尝试起飞并获取位置信息，导致起飞与定位技能均立即失败，任务整体未开始即告失败。 -- 失败的核心原因是执行任务前无人机已处于 status=error 状态，却仍直接调用 takeoff 和 get_position；在机体处于错误状态时，底层控制/状态查询接口不可用或直接返回失败，导致技能调用在0.0s内即失败，任务没有进入实际飞行与感知阶段。缺乏对初始状态的前置检查和异常恢复逻辑是根本问题。
  - 在规划任何飞行或感知任务前，应首先检查 UAV 状态字段（如 status），若为 error/critical 等异常值，应终止当前任务并尝试调用专门的自检/复位流程，而不是直接调用任务技能。
  - 对于起飞类任务，将前置条件显式化：需要 status 为 ready/idle/ok 且当前高度低于目标高度；否则应优先处理状态异常而不是重复尝试 takeoff。
  - 在技能历史记录为空、且首次调用即 0.0s 失败时，应优先考虑系统状态或接口初始化问题，而不是参数不当，后续规划应插入诊断步骤（查询详细错误、重启控制模块等）。
  - 任务目标是“观察周围环境”时，如果起飞阶段失败，应向上汇报“起飞阶段未完成，因此无法进行观察”，而不是尝试执行后续观察或定位技能，以避免级联失败。

- [2026-03-13 15:27] 无人机顺利起飞并稳定爬升至约15米高度完成环境观察任务。 -- 任务一次规划执行完成，无障碍、无重规划、无应急情况，表明在正常状态（status=idle、电量充足）下，基础飞行与观察技能链路稳定可靠。
  - 在 UAV 状态为 idle 且电量充足的情况下，可直接执行由 takeoff → change_altitude → observe 组成的基础侦察任务链，无需预先插入额外诊断步骤。
  - 观察类任务的总时长受 observe 技能耗时影响较大，后续规划类似任务时应为感知阶段预留足够时间预算。
  - 在无障碍物报告、无重规划的情形下，固定高度（如约15m）可以作为安全而高效的默认侦察高度基线。

- [2026-03-13 15:27] 无人机顺利起飞并稳定爬升至约15米高度完成环境观察任务。 -- 任务一次规划执行完成，无障碍、无重规划、无应急情况，表明在正常状态（status=idle、电量充足）下，基础飞行与观察技能链路稳定可靠。
  - 在 UAV 状态为 idle 且电量充足的情况下，可直接执行由 takeoff → change_altitude → observe 组成的基础侦察任务链，无需预先插入额外诊断步骤。
  - 观察类任务的总时长受 observe 技能耗时影响较大，后续规划类似任务时应为感知阶段预留足够时间预算。
  - 在无障碍物报告、无重规划的情形下，固定高度（如约15m）可以作为安全而高效的默认侦察高度基线。

- [2026-03-13 15:36] 在废墟区域搜索任务中，多次移动与观察后未能有效定位和成功救援被困人员，任务宣告失败。 -- 核心失败原因在于感知与救援决策链条不完整：首次 rescue_person 在缺乏有效目标锁定或前置条件检查的情况下直接调用即刻失败，随后的多次 observe 虽有成功记录但缺乏基于观测结果的策略调整和重新定位救援目标的逻辑，导致整条流程虽然飞行与观察执行正常，却没有闭环到“发现-确认-救援”的完整链路；同时没有重规划机制来在多次观察失败后改变搜索模式或区域。
  - 在调用救援类技能（如 rescue_person）前，应确保已通过 observe 等感知技能获取到明确的目标信息或候选位置，否则容易出现耗时为0的立即失败，需要在规划阶段增加“目标已锁定/已验证”的前置条件检查。
  - 搜索类任务应形成“移动-观察-结果判定-策略调整”的闭环逻辑：每次 observe 后需要显式评估是否发现目标，并根据结果决定是局部精细搜索、扩大搜索范围还是改变高度和视角，而不是机械重复飞行与观察。
  - 连续多次 observe 失败后应触发搜索策略升级或重规划（例如改变搜索路径形状、调整高度、扩大范围），而不是继续沿用原模式；可设置失败次数/时间的阈值作为重规划触发条件。
  - 对于时间较长的搜索任务，要在规划中预留对 battery 的关注和阈值，例如约定在电量低于一定比例前（如30%）提前结束本轮搜索或返回，以避免在电量仍较高时却没有有效利用搜索时间。
  - 在没有感知事件与历史统计可用的早期阶段，规划时应更保守地增加中间检查点（如查看当前状态、确认传感器是否返回有效数据）来逐步摸清技能实际表现与限制。
  - 对于“搜索并救援”类任务，应将救援行为拆分为至少三个阶段：候选目标检测（observe）、目标确认/精细观察（多次 observe + 距离/角度调整）、执行救援（rescue_person），避免直接从粗略搜索跳到救援。

- [2026-03-13 15:36] 在废墟区域搜索任务中，多次移动与观察后未能有效定位和成功救援被困人员，任务宣告失败。 -- 核心失败原因在于感知与救援决策链条不完整：首次 rescue_person 在缺乏有效目标锁定或前置条件检查的情况下直接调用即刻失败，随后的多次 observe 虽有成功记录但缺乏基于观测结果的策略调整和重新定位救援目标的逻辑，导致整条流程虽然飞行与观察执行正常，却没有闭环到“发现-确认-救援”的完整链路；同时没有重规划机制来在多次观察失败后改变搜索模式或区域。
  - 在调用救援类技能（如 rescue_person）前，应确保已通过 observe 等感知技能获取到明确的目标信息或候选位置，否则容易出现耗时为0的立即失败，需要在规划阶段增加“目标已锁定/已验证”的前置条件检查。
  - 搜索类任务应形成“移动-观察-结果判定-策略调整”的闭环逻辑：每次 observe 后需要显式评估是否发现目标，并根据结果决定是局部精细搜索、扩大搜索范围还是改变高度和视角，而不是机械重复飞行与观察。
  - 连续多次 observe 失败后应触发搜索策略升级或重规划（例如改变搜索路径形状、调整高度、扩大范围），而不是继续沿用原模式；可设置失败次数/时间的阈值作为重规划触发条件。
  - 对于时间较长的搜索任务，要在规划中预留对 battery 的关注和阈值，例如约定在电量低于一定比例前（如30%）提前结束本轮搜索或返回，以避免在电量仍较高时却没有有效利用搜索时间。
  - 在没有感知事件与历史统计可用的早期阶段，规划时应更保守地增加中间检查点（如查看当前状态、确认传感器是否返回有效数据）来逐步摸清技能实际表现与限制。
  - 对于“搜索并救援”类任务，应将救援行为拆分为至少三个阶段：候选目标检测（observe）、目标确认/精细观察（多次 observe + 距离/角度调整）、执行救援（rescue_person），避免直接从粗略搜索跳到救援。

- [2026-03-13 15:54] 在一次废墟区域巡检中，无人机完成多轮飞行与观察但未成功发现被困人员，任务判定失败。 -- 失败的核心原因不在于单个技能执行错误，而在于搜索策略不足：在有限时间内虽进行了多次 observe 和少量位移，但缺乏系统性的覆盖规划、结果驱动的决策（基于观察结果主动调整搜索区域和高度），以及在发现困难时触发重规划或更换搜索模式，没有形成“感知-判断-再规划”的闭环。
  - 废墟搜索类任务需要显式的区域覆盖策略（例如网格式/条带式/螺旋式搜索），不能仅依赖少量 fly_to + look_around + observe 的组合，否则容易出现局部反复观察但总体覆盖不足的问题。
  - 多次连续 observe 成功但仍未找到目标时，应以次数或时间为触发条件进行策略升级：例如提高/降低高度、改变视角（侧视/俯视）、扩大搜索半径，而不是继续在同一模式下观察。
  - 单次 observe 失败（如本次 1 次 FAIL）在整体任务中并非致命，但应作为信号：考虑传感器状态、光照遮挡或视角不佳，可自动触发补拍（同位置再次 observe）或微小位移 + observe，而不是简单重试相同动作。
  - 在没有障碍、无应急停止且电量充足的情况下，重规划次数为 0 反映上层逻辑偏“线性脚本化”；对于搜索救援类任务，应在规划层加入基于任务进展的中途评估和重规划机制（如：N 秒内未发现目标则切换到更大范围扫描）。
  - look_around 适合在关键位置执行一次以快速建立局部环境认知，但不应替代系统的多点位搜索；在废墟任务中应将 look_around 用作“侦察节点”，侦察结果应驱动后续 fly_relative / fly_to 构建覆盖路径。
  - 任务结束前应检查是否存在可继续利用的资源（例如当前电量、时间预算），若仍有较大余量且未发现目标，应自动延长搜索并扩大范围，而不应过早结束任务。
  - 在任务记录中缺乏对 observe/ look_around 结果的结构化反馈（例如检测到可疑目标、无遮挡区域比例等），未来应在规划逻辑中显式依赖这些感知结果来决定是否移动、改变高度或结束搜索。

- [2026-03-13 15:54] 在一次废墟区域巡检中，无人机完成多轮飞行与观察但未成功发现被困人员，任务判定失败。 -- 失败的核心原因不在于单个技能执行错误，而在于搜索策略不足：在有限时间内虽进行了多次 observe 和少量位移，但缺乏系统性的覆盖规划、结果驱动的决策（基于观察结果主动调整搜索区域和高度），以及在发现困难时触发重规划或更换搜索模式，没有形成“感知-判断-再规划”的闭环。
  - 废墟搜索类任务需要显式的区域覆盖策略（例如网格式/条带式/螺旋式搜索），不能仅依赖少量 fly_to + look_around + observe 的组合，否则容易出现局部反复观察但总体覆盖不足的问题。
  - 多次连续 observe 成功但仍未找到目标时，应以次数或时间为触发条件进行策略升级：例如提高/降低高度、改变视角（侧视/俯视）、扩大搜索半径，而不是继续在同一模式下观察。
  - 单次 observe 失败（如本次 1 次 FAIL）在整体任务中并非致命，但应作为信号：考虑传感器状态、光照遮挡或视角不佳，可自动触发补拍（同位置再次 observe）或微小位移 + observe，而不是简单重试相同动作。
  - 在没有障碍、无应急停止且电量充足的情况下，重规划次数为 0 反映上层逻辑偏“线性脚本化”；对于搜索救援类任务，应在规划层加入基于任务进展的中途评估和重规划机制（如：N 秒内未发现目标则切换到更大范围扫描）。
  - look_around 适合在关键位置执行一次以快速建立局部环境认知，但不应替代系统的多点位搜索；在废墟任务中应将 look_around 用作“侦察节点”，侦察结果应驱动后续 fly_relative / fly_to 构建覆盖路径。
  - 任务结束前应检查是否存在可继续利用的资源（例如当前电量、时间预算），若仍有较大余量且未发现目标，应自动延长搜索并扩大范围，而不应过早结束任务。
  - 在任务记录中缺乏对 observe/ look_around 结果的结构化反馈（例如检测到可疑目标、无遮挡区域比例等），未来应在规划逻辑中显式依赖这些感知结果来决定是否移动、改变高度或结束搜索。

- [2026-03-13 16:10] 在废墟区域的空中搜索任务中，多次成功飞行与观察，但在后续一次 takeoff 调用异常失败，整体未能有效发现或确认被困人员位置，任务判定失败。 -- 从技能链看，大部分飞行与观察技能均成功执行，且未遇障碍、无应急停止，说明环境相对通畅，平台状态（battery=68%，status=idle）也正常；任务失败的关键原因主要是搜索策略与终止条件设计不足：一方面缺乏对搜索覆盖范围与停止条件的规划与在线评估（没有显式的“已覆盖区域是否足够”“是否需要扩大范围/提高分辨率”判断）；另一方面在倒数阶段出现一次 takeoff[FAIL] 且耗时为0.0s，系统未对该异常进行处理或分支规划（未做状态检查、重试或改用其他动作），但仍继续执行后续技能，表明对关键动作失败缺乏全局级联影响的处理逻辑，这导致流程完成但任务目标（发现并确认被困人员）未达成。
  - 在搜索类任务中，需要在规划阶段明确“覆盖指标”和终止条件，例如预设需要扫过的区域网格、航线长度或最小观察点数量，避免仅按固定几次飞行和观察后就草率结束任务。
  - 对关键技能（如 takeoff）出现耗时为0.0s且立即失败的情况，要视为高危信号：应立即触发状态检查或降级流程，而不是简单忽略并继续执行后续技能。
  - 在任务执行过程中，应周期性检查 UAV 的 status 与 battery 等状态，即使当前状态为 idle 也要在关键阶段（起飞前、长距离飞行前）再次确认，以便在状态异常时提前调整计划。
  - 在缺乏感知事件和历史统计的情况下，规划搜索路线更应采用结构化巡检策略（如条带式扫描、螺旋式扩张等），而不是仅靠少量 fly_to + fly_relative 组合，确保空间覆盖具有可解释性和可复现性。
  - 当任务目标是“寻找被困人员”这类需要识别目标的场景，observe / look_around 等感知技能的调用应配合判定逻辑（例如阈值式的置信度检测或多次确认），而不是只执行感知动作却不对结果进行任务层面的决策更新。

- [2026-03-13 16:10] 在废墟区域的空中搜索任务中，多次成功飞行与观察，但在后续一次 takeoff 调用异常失败，整体未能有效发现或确认被困人员位置，任务判定失败。 -- 从技能链看，大部分飞行与观察技能均成功执行，且未遇障碍、无应急停止，说明环境相对通畅，平台状态（battery=68%，status=idle）也正常；任务失败的关键原因主要是搜索策略与终止条件设计不足：一方面缺乏对搜索覆盖范围与停止条件的规划与在线评估（没有显式的“已覆盖区域是否足够”“是否需要扩大范围/提高分辨率”判断）；另一方面在倒数阶段出现一次 takeoff[FAIL] 且耗时为0.0s，系统未对该异常进行处理或分支规划（未做状态检查、重试或改用其他动作），但仍继续执行后续技能，表明对关键动作失败缺乏全局级联影响的处理逻辑，这导致流程完成但任务目标（发现并确认被困人员）未达成。
  - 在搜索类任务中，需要在规划阶段明确“覆盖指标”和终止条件，例如预设需要扫过的区域网格、航线长度或最小观察点数量，避免仅按固定几次飞行和观察后就草率结束任务。
  - 对关键技能（如 takeoff）出现耗时为0.0s且立即失败的情况，要视为高危信号：应立即触发状态检查或降级流程，而不是简单忽略并继续执行后续技能。
  - 在任务执行过程中，应周期性检查 UAV 的 status 与 battery 等状态，即使当前状态为 idle 也要在关键阶段（起飞前、长距离飞行前）再次确认，以便在状态异常时提前调整计划。
  - 在缺乏感知事件和历史统计的情况下，规划搜索路线更应采用结构化巡检策略（如条带式扫描、螺旋式扩张等），而不是仅靠少量 fly_to + fly_relative 组合，确保空间覆盖具有可解释性和可复现性。
  - 当任务目标是“寻找被困人员”这类需要识别目标的场景，observe / look_around 等感知技能的调用应配合判定逻辑（例如阈值式的置信度检测或多次确认），而不是只执行感知动作却不对结果进行任务层面的决策更新。

- [2026-03-13 16:25] 本次巡检废墟区域任务在无障碍、单次规划下顺利完成并安全返航。 -- 任务顺利完成的核心原因是：环境较为简单（无障碍、无感知异常）、UAV 状态正常且电量充足，加上合理的巡检路径规划（多次观察与环顾结合）和一次性计划执行，无需重规划或应急停止。
  - 在环境无明显障碍和威胁的情况下，采用“观察/环顾 → 分段飞行巡检 → 再次观察/标记 → 返航”的线性流程足以完成巡检任务，无需引入复杂的自适应规划。
  - 巡检类任务中前后多次交替使用 observe 和 look_around 能提升区域覆盖与信息完整性，可作为废墟巡检的默认感知模式。
  - return_to_launch 的时间占比较大（约一半总耗时），说明规划任务路径时应考虑返航路程，将任务区尽量靠近起降点或在同一区域集中完成多个子任务以提高整体效率。
  - 在无障碍情况下，连续多段短程 fly_to 能稳定完成区域巡检，可将这一模式作为未知但开阔区域的安全默认飞行策略。
  - mark_location 的开销几乎为零，可以在关键观察点放心使用，不必担心对任务总时长产生明显影响。

- [2026-03-13 16:25] 本次巡检废墟区域任务在无障碍、单次规划下顺利完成并安全返航。 -- 任务顺利完成的核心原因是：环境较为简单（无障碍、无感知异常）、UAV 状态正常且电量充足，加上合理的巡检路径规划（多次观察与环顾结合）和一次性计划执行，无需重规划或应急停止。
  - 在环境无明显障碍和威胁的情况下，采用“观察/环顾 → 分段飞行巡检 → 再次观察/标记 → 返航”的线性流程足以完成巡检任务，无需引入复杂的自适应规划。
  - 巡检类任务中前后多次交替使用 observe 和 look_around 能提升区域覆盖与信息完整性，可作为废墟巡检的默认感知模式。
  - return_to_launch 的时间占比较大（约一半总耗时），说明规划任务路径时应考虑返航路程，将任务区尽量靠近起降点或在同一区域集中完成多个子任务以提高整体效率。
  - 在无障碍情况下，连续多段短程 fly_to 能稳定完成区域巡检，可将这一模式作为未知但开阔区域的安全默认飞行策略。
  - mark_location 的开销几乎为零，可以在关键观察点放心使用，不必担心对任务总时长产生明显影响。

- [2026-03-13 17:34] 任务在起飞阶段即失败，无人机未能开始飞向目标坐标。 -- 失败的直接原因是 takeoff 技能在调用时即刻失败，根本原因是执行前无人机状态已为 status=error，却仍直接尝试飞行技能，缺乏对错误状态的检测与恢复步骤。
  - 在调用任何飞行或导航类技能前，必须首先检查 UAV 的 status 字段；若为 error，应中止本次飞行计划并转入诊断/恢复流程，而不是继续调用飞行技能。
  - 即便电量和位置数据看似正常，只要 status=error，就要将当前飞行能力视为不可用，不应尝试起飞或导航。
  - 当技能历史为空且首次操作即失败时，应增加对系统健康状态的显式检查逻辑，而不是假设默认可用。

- [2026-03-13 17:34] 任务在起飞阶段即失败，无人机未能开始飞向目标坐标。 -- 失败的直接原因是 takeoff 技能在调用时即刻失败，根本原因是执行前无人机状态已为 status=error，却仍直接尝试飞行技能，缺乏对错误状态的检测与恢复步骤。
  - 在调用任何飞行或导航类技能前，必须首先检查 UAV 的 status 字段；若为 error，应中止本次飞行计划并转入诊断/恢复流程，而不是继续调用飞行技能。
  - 即便电量和位置数据看似正常，只要 status=error，就要将当前飞行能力视为不可用，不应尝试起飞或导航。
  - 当技能历史为空且首次操作即失败时，应增加对系统健康状态的显式检查逻辑，而不是假设默认可用。

- [2026-03-13 17:38] 任务在起飞阶段失败，无人机未能开始对废墟区域上空的巡视。 -- 失败的直接原因是在 UAV 状态为 error 的情况下仍然尝试执行 takeoff，导致技能立即失败；根本原因是任务规划前未对 UAV 状态进行健康检查和故障恢复判断。
  - 在规划任何飞行任务前，应首先检查 UAV 的 status 字段；若为 error，则应中止飞行类任务并优先触发诊断/恢复流程，而不是直接调用飞行技能。
  - 即使无人机位置 pos 和电量 battery 显示正常，只要 status!=正常(如 error)，就不能假定飞控和执行链路可用，飞行任务应视为不具备前置条件。
  - 对于只有单一关键技能(如 takeoff)的简短任务，如果该技能失败，应在规划层记录失败原因（例如状态异常），避免重复无效重试。

- [2026-03-13 17:38] 任务在起飞阶段失败，无人机未能开始对废墟区域上空的巡视。 -- 失败的直接原因是在 UAV 状态为 error 的情况下仍然尝试执行 takeoff，导致技能立即失败；根本原因是任务规划前未对 UAV 状态进行健康检查和故障恢复判断。
  - 在规划任何飞行任务前，应首先检查 UAV 的 status 字段；若为 error，则应中止飞行类任务并优先触发诊断/恢复流程，而不是直接调用飞行技能。
  - 即使无人机位置 pos 和电量 battery 显示正常，只要 status!=正常(如 error)，就不能假定飞控和执行链路可用，飞行任务应视为不具备前置条件。
  - 对于只有单一关键技能(如 takeoff)的简短任务，如果该技能失败，应在规划层记录失败原因（例如状态异常），避免重复无效重试。

- [2026-03-13 18:49] 任务在起飞阶段即失败，无人机未能执行任何实质飞行动作。 -- 失败的核心原因是 UAV 当前状态为 status=error，导致起飞技能在调用时立即失败且无实际执行时间，规划中没有在调用飞行技能前对错误状态进行检测和恢复。
  - 在调用任何飞行相关技能（如 takeoff、move_to）前，应检查 UAV status 是否为正常运行状态（例如 ready/ok），若为 error 则应先执行诊断或恢复流程，而不是直接尝试飞行。
  - 即使电量充足且已有位置高度数据，只要状态为 error，就应视为飞行链路不可用，不应盲目重试飞行技能，以免重复出现 0 耗时失败。
  - 在任务规划阶段应加入前置健康检查步骤：若发现状态异常，应中止本次任务并返回明确的错误说明，而不是仅记录技能失败。

- [2026-03-13 18:49] 任务在起飞阶段即失败，无人机未能执行任何实质飞行动作。 -- 失败的核心原因是 UAV 当前状态为 status=error，导致起飞技能在调用时立即失败且无实际执行时间，规划中没有在调用飞行技能前对错误状态进行检测和恢复。
  - 在调用任何飞行相关技能（如 takeoff、move_to）前，应检查 UAV status 是否为正常运行状态（例如 ready/ok），若为 error 则应先执行诊断或恢复流程，而不是直接尝试飞行。
  - 即使电量充足且已有位置高度数据，只要状态为 error，就应视为飞行链路不可用，不应盲目重试飞行技能，以免重复出现 0 耗时失败。
  - 在任务规划阶段应加入前置健康检查步骤：若发现状态异常，应中止本次任务并返回明确的错误说明，而不是仅记录技能失败。

- [2026-03-13 19:38] 无人机从当前空中位置调整至15米高度后飞往指定坐标并完成观察与环视，任务顺利完成且无障碍与重规划。 -- 任务一次规划即成功完成，所有技能按预期顺序执行且无故障、无障碍、无应急停止，表明在当前环境和状态为idle、剩余电量充足的条件下，该类『变高度+直线飞行+观察/环视』组合流程是稳定可靠的。
  - 在 status=idle 且电量充足的情况下，可直接采用『change_altitude → fly_to → observe → look_around → hover』的标准侦察链路，无需额外健康检查即可高效完成中短程观察任务。
  - 观察类任务中，感知技能（observe + look_around）的时间占比很大（约70%以上），规划时需要为感知阶段预留充足时间，飞行时间通常不是瓶颈。
  - 在无障碍提示、无重规划的静态环境下，简单的单段 fly_to 足以完成从起飞点到目标点的位移，无需切分为多段路径或进行复杂路径规划。
  - 任务结束时悬停（hover）可作为安全的收尾手段，为后续决策（继续任务/返航/进一步观察）保留机动空间。

- [2026-03-13 19:38] 无人机从当前空中位置调整至15米高度后飞往指定坐标并完成观察与环视，任务顺利完成且无障碍与重规划。 -- 任务一次规划即成功完成，所有技能按预期顺序执行且无故障、无障碍、无应急停止，表明在当前环境和状态为idle、剩余电量充足的条件下，该类『变高度+直线飞行+观察/环视』组合流程是稳定可靠的。
  - 在 status=idle 且电量充足的情况下，可直接采用『change_altitude → fly_to → observe → look_around → hover』的标准侦察链路，无需额外健康检查即可高效完成中短程观察任务。
  - 观察类任务中，感知技能（observe + look_around）的时间占比很大（约70%以上），规划时需要为感知阶段预留充足时间，飞行时间通常不是瓶颈。
  - 在无障碍提示、无重规划的静态环境下，简单的单段 fly_to 足以完成从起飞点到目标点的位移，无需切分为多段路径或进行复杂路径规划。
  - 任务结束时悬停（hover）可作为安全的收尾手段，为后续决策（继续任务/返航/进一步观察）保留机动空间。

- [2026-03-13 19:40] 任务通过一次调用 get_position 技能成功获取了当前 UAV 的位置信息。 -- 任务需求与所选技能高度匹配，环境状态正常（status=idle），避免了此前在 status=error 场景下的失败模式，因此一次调用即成功完成任务。
  - 针对简单的状态查询类任务（如询问当前位置），优先直接调用对应的单一状态技能即可，无需额外规划和冗余检查。
  - 在选择执行状态查询技能前，应确认 UAV 不处于 error 状态；若状态为 idle 且无异常标记，可以假定状态查询成本低且成功率高。
  - 对无历史统计记录的新环境/新任务，先从无副作用、只读的状态技能入手可以安全验证控制链路是否可用。

- [2026-03-13 19:40] 任务通过一次调用 get_position 技能成功获取了当前 UAV 的位置信息。 -- 任务需求与所选技能高度匹配，环境状态正常（status=idle），避免了此前在 status=error 场景下的失败模式，因此一次调用即成功完成任务。
  - 针对简单的状态查询类任务（如询问当前位置），优先直接调用对应的单一状态技能即可，无需额外规划和冗余检查。
  - 在选择执行状态查询技能前，应确认 UAV 不处于 error 状态；若状态为 idle 且无异常标记，可以假定状态查询成本低且成功率高。
  - 对无历史统计记录的新环境/新任务，先从无副作用、只读的状态技能入手可以安全验证控制链路是否可用。

- [2026-03-13 19:44] 无人机在执行全区域巡视任务时，多次局部观察和位移但未能成功完成核心的巡逻覆盖规划，导致任务失败。 -- 任务失败的核心原因是高层巡逻技能 patrol_area 在调用时即刻失败（耗时为0.0s），说明该关键技能未成功初始化或前置条件不满足；随后虽通过多次 look_around 和 fly_relative 进行局部探索，但缺乏系统化的区域覆盖策略与任务完成判据，导致在未检测到火灾或异常的情况下也无法宣告任务完成。此外，没有感知事件与重规划记录，表明在关键高层技能失败后没有触发基于失败原因的自适应重规划或降级方案。
  - 对关键高层技能（如 patrol_area）要在规划阶段显式检查其前置条件（地图/区域边界是否已知、传感器状态、飞行高度和安全区设置等），一旦调用即刻失败（耗时≈0）要视为配置错误或前置条件不满足，而非环境问题。
  - 当任务目标是“巡视整个区域”这类需要覆盖性的目标时，如果高层巡逻技能失败，应启用降级方案：基于当前位姿和大致区域边界采用规则化栅格/蛇形路径，用 fly_relative + hover + look_around 组合近似实现覆盖，并且维护已覆盖区域记录，而不是只做几次局部移动和观察。
  - 在缺少感知事件的情况下，应区分“未发现异常（正常完成）”与“未能覆盖区域（任务失败）”：需要在任务层维护覆盖率或关键检查点的完成度指标，而不是仅依赖感知事件是否触发。
  - 当某个关键技能第一次出现 0 耗时失败时，应在同一任务中减少重复尝试该技能，而是记录该失败并触发策略切换（降级巡逻、参数检查或任务中止并上报配置问题）。
  - 对于巡视任务，要在规划中显式设定终止条件：例如预设覆盖路径完成、覆盖率估计达到阈值或到达所有规划检查点，避免在没有障碍和告警的情况下陷入“飞几步-观察几次但永不结束”的状态。
  - 在高层任务执行过程中，应定期检查任务时间与电量占用情况；本次电量尚充足但任务接近两分钟仍未建立有效巡逻模式，未来应在时间上设置早期异常检测（例如关键技能多次即刻失败+时间超阈值→判定为策略问题并中止）。

- [2026-03-13 19:44] 无人机在执行全区域巡视任务时，多次局部观察和位移但未能成功完成核心的巡逻覆盖规划，导致任务失败。 -- 任务失败的核心原因是高层巡逻技能 patrol_area 在调用时即刻失败（耗时为0.0s），说明该关键技能未成功初始化或前置条件不满足；随后虽通过多次 look_around 和 fly_relative 进行局部探索，但缺乏系统化的区域覆盖策略与任务完成判据，导致在未检测到火灾或异常的情况下也无法宣告任务完成。此外，没有感知事件与重规划记录，表明在关键高层技能失败后没有触发基于失败原因的自适应重规划或降级方案。
  - 对关键高层技能（如 patrol_area）要在规划阶段显式检查其前置条件（地图/区域边界是否已知、传感器状态、飞行高度和安全区设置等），一旦调用即刻失败（耗时≈0）要视为配置错误或前置条件不满足，而非环境问题。
  - 当任务目标是“巡视整个区域”这类需要覆盖性的目标时，如果高层巡逻技能失败，应启用降级方案：基于当前位姿和大致区域边界采用规则化栅格/蛇形路径，用 fly_relative + hover + look_around 组合近似实现覆盖，并且维护已覆盖区域记录，而不是只做几次局部移动和观察。
  - 在缺少感知事件的情况下，应区分“未发现异常（正常完成）”与“未能覆盖区域（任务失败）”：需要在任务层维护覆盖率或关键检查点的完成度指标，而不是仅依赖感知事件是否触发。
  - 当某个关键技能第一次出现 0 耗时失败时，应在同一任务中减少重复尝试该技能，而是记录该失败并触发策略切换（降级巡逻、参数检查或任务中止并上报配置问题）。
  - 对于巡视任务，要在规划中显式设定终止条件：例如预设覆盖路径完成、覆盖率估计达到阈值或到达所有规划检查点，避免在没有障碍和告警的情况下陷入“飞几步-观察几次但永不结束”的状态。
  - 在高层任务执行过程中，应定期检查任务时间与电量占用情况；本次电量尚充足但任务接近两分钟仍未建立有效巡逻模式，未来应在时间上设置早期异常检测（例如关键技能多次即刻失败+时间超阈值→判定为策略问题并中止）。

- [2026-03-13 19:50] 无人机在巡逻任务中多次感知与位姿查询失败，最终以错误状态结束，未完成区域巡视与报告。 -- 任务失败的核心原因是关键感知技能（look_around）和状态获取技能（get_position）在任务中后段持续失败，导致无法完成对整个区域的覆盖检查和精确报告位置；同时 patrol_area 复合技能在启动阶段即失败，表明对底层技能状态与 UAV 整体健康状态缺乏前置检查与降级策略。
  - 在执行类似“巡视整个区域”这种复合任务前，应先验证构成该任务的关键基础技能（如 look_around、get_position）的最近一次执行是否稳定，否则需要缩小任务范围或改用更保守的方案。
  - 对于需要“发现后报告位置”的任务，应在飞行动作链中周期性调用 get_position 并在其首次或连续失败时触发保护逻辑（如原地悬停并尝试恢复，而不是继续后续动作）。
  - 当巡逻任务中途出现关键感知技能长时间执行但失败（如 look_around 耗时远高于平均值且结果为 FAIL）时，应提前判定任务整体难以完成并中止任务，而不是继续飞行以避免无效巡逻。
  - 在没有障碍和应急停止的情况下仍然任务失败，说明规划层应增加对“传感/状态链路”类失败的监控与处理，而不仅仅关注物理避障与航迹执行。

- [2026-03-13 19:50] 无人机在巡逻任务中多次感知与位姿查询失败，最终以错误状态结束，未完成区域巡视与报告。 -- 任务失败的核心原因是关键感知技能（look_around）和状态获取技能（get_position）在任务中后段持续失败，导致无法完成对整个区域的覆盖检查和精确报告位置；同时 patrol_area 复合技能在启动阶段即失败，表明对底层技能状态与 UAV 整体健康状态缺乏前置检查与降级策略。
  - 在执行类似“巡视整个区域”这种复合任务前，应先验证构成该任务的关键基础技能（如 look_around、get_position）的最近一次执行是否稳定，否则需要缩小任务范围或改用更保守的方案。
  - 对于需要“发现后报告位置”的任务，应在飞行动作链中周期性调用 get_position 并在其首次或连续失败时触发保护逻辑（如原地悬停并尝试恢复，而不是继续后续动作）。
  - 当巡逻任务中途出现关键感知技能长时间执行但失败（如 look_around 耗时远高于平均值且结果为 FAIL）时，应提前判定任务整体难以完成并中止任务，而不是继续飞行以避免无效巡逻。
  - 在没有障碍和应急停止的情况下仍然任务失败，说明规划层应增加对“传感/状态链路”类失败的监控与处理，而不仅仅关注物理避障与航迹执行。

- [2026-03-13 20:03] 无人机顺利完成起飞至12米、向东飞行30米观测并返回起飞点降落的任务。 -- 任务一次性成功且无重规划、无障碍与应急中断，表明在初始状态正常、电量充足、环境简单的条件下，当前标准飞行与返航流程是稳定且可靠的。
  - 在 UAV 状态为 idle 且电量充足（>90%）的情况下，可直接执行标准链路：takeoff → change_altitude → fly_to → observe → return_to_launch，无需额外健康检查。
  - 返航阶段可能占据任务总时间的大头（本次约60%），在未来规划中应显式考虑返航距离与时间预算，而不是只关注前向航程。
  - 简单无障碍环境中，未使用避障与重规划策略也能完成任务，表明在明确短航程、开阔区域任务中可以采用轻量规划以降低复杂度。
  - 起飞高度调整与短距平飞组合在时间与稳定性上表现良好，可以作为类似“起飞—短距探查—返回”任务的默认结构模板。

- [2026-03-13 20:03] 无人机顺利完成起飞至12米、向东飞行30米观测并返回起飞点降落的任务。 -- 任务一次性成功且无重规划、无障碍与应急中断，表明在初始状态正常、电量充足、环境简单的条件下，当前标准飞行与返航流程是稳定且可靠的。
  - 在 UAV 状态为 idle 且电量充足（>90%）的情况下，可直接执行标准链路：takeoff → change_altitude → fly_to → observe → return_to_launch，无需额外健康检查。
  - 返航阶段可能占据任务总时间的大头（本次约60%），在未来规划中应显式考虑返航距离与时间预算，而不是只关注前向航程。
  - 简单无障碍环境中，未使用避障与重规划策略也能完成任务，表明在明确短航程、开阔区域任务中可以采用轻量规划以降低复杂度。
  - 起飞高度调整与短距平飞组合在时间与稳定性上表现良好，可以作为类似“起飞—短距探查—返回”任务的默认结构模板。

- [2026-03-13 20:05] 无人机顺利起飞到约15米高度，飞至指定坐标附近并通过多次观测成功完成亮红色发光物体的搜索与标记任务。 -- 此次任务从起飞到搜索与观测全流程一次规划顺利执行，无重规划与异常中断，表明在 UAV 状态正常且环境无明显障碍的条件下，当前飞行与观测技能链路配置稳定可靠，能满足中短距离定点搜索和多次观测的需求。
  - 在状态正常（status=idle）、电量充足的前提下，基础链路“takeoff -> fly_to -> 多次 observe -> mark_location”对简单定点搜索任务是稳定可用的，无需复杂的重规划策略。
  - 对地面目标的搜索使用多次 observe 调用是有效策略：先进行广域搜索，再在找到疑似目标后追加面向性的向下观测，可提高发现和确认目标的可靠性。
  - 终态中 UAV 位姿与目标坐标接近（水平偏差在米级范围内）且任务成功，说明在此类搜索任务中对飞到坐标的精度要求不必过于苛刻，只要在若干米范围内即可满足后续视觉搜索需要。
  - 任务在约50秒内完成且剩余电量仍有较高比例，表明类似规模的单点搜索任务在时间与能耗上安全裕度较大，可以在规划时适当分配更多观测时间以换取识别可靠性。

- [2026-03-13 20:05] 无人机顺利起飞到约15米高度，飞至指定坐标附近并通过多次观测成功完成亮红色发光物体的搜索与标记任务。 -- 此次任务从起飞到搜索与观测全流程一次规划顺利执行，无重规划与异常中断，表明在 UAV 状态正常且环境无明显障碍的条件下，当前飞行与观测技能链路配置稳定可靠，能满足中短距离定点搜索和多次观测的需求。
  - 在状态正常（status=idle）、电量充足的前提下，基础链路“takeoff -> fly_to -> 多次 observe -> mark_location”对简单定点搜索任务是稳定可用的，无需复杂的重规划策略。
  - 对地面目标的搜索使用多次 observe 调用是有效策略：先进行广域搜索，再在找到疑似目标后追加面向性的向下观测，可提高发现和确认目标的可靠性。
  - 终态中 UAV 位姿与目标坐标接近（水平偏差在米级范围内）且任务成功，说明在此类搜索任务中对飞到坐标的精度要求不必过于苛刻，只要在若干米范围内即可满足后续视觉搜索需要。
  - 任务在约50秒内完成且剩余电量仍有较高比例，表明类似规模的单点搜索任务在时间与能耗上安全裕度较大，可以在规划时适当分配更多观测时间以换取识别可靠性。

- [2026-03-13 20:14] 无人机完成观察和返航流程后在执行降落时连续失败，任务整体判定为失败。 -- 任务主体流程（观察、环视、标记位置、返航）均成功，失败集中在末尾两次 land 技能调用均瞬时失败且耗时为 0.0s，说明在当前世界状态下降落动作不可执行或接口异常；规划层在第一次 land 失败后仅重复调用同一技能一次，没有尝试替代流程（如继续保持悬停、诊断状态或切换安全模式），导致在可控流程基本完成的情况下仍以失败收尾。
  - 遇到关键安全技能（如 land）连续 0.0s 耗时失败时，规划层不应简单重复调用，应先调用状态检测技能（如 get_position、get_status）确认当前高度、模式与状态，再决定是否需要降落、悬停或终止任务。
  - 返航成功后应先检查无人机当前高度和状态，如果已经处于接近地面或被认为 idle/grounded 状态，可以跳过显式的 land 调用或采用条件式降落逻辑（例如：高度阈值之上才执行 land）。
  - 对任务结果判定应与实际风险相匹配：如果观测和返航目标全部完成，只是降落接口在“已着陆/不需要降落”状态下返回失败，应考虑区分“任务目标失败”和“结束动作异常”，避免过度将任务标记为整体失败。
  - 在没有重规划与应急停止触发的情况下发生失败，说明当前策略对异常缺乏分支处理。以后在关键步骤（起飞、降落、模式切换）失败时，应尝试至少一次高层级的恢复策略或安全检查，而不是直接结束任务。

- [2026-03-13 20:14] 无人机完成观察和返航流程后在执行降落时连续失败，任务整体判定为失败。 -- 任务主体流程（观察、环视、标记位置、返航）均成功，失败集中在末尾两次 land 技能调用均瞬时失败且耗时为 0.0s，说明在当前世界状态下降落动作不可执行或接口异常；规划层在第一次 land 失败后仅重复调用同一技能一次，没有尝试替代流程（如继续保持悬停、诊断状态或切换安全模式），导致在可控流程基本完成的情况下仍以失败收尾。
  - 遇到关键安全技能（如 land）连续 0.0s 耗时失败时，规划层不应简单重复调用，应先调用状态检测技能（如 get_position、get_status）确认当前高度、模式与状态，再决定是否需要降落、悬停或终止任务。
  - 返航成功后应先检查无人机当前高度和状态，如果已经处于接近地面或被认为 idle/grounded 状态，可以跳过显式的 land 调用或采用条件式降落逻辑（例如：高度阈值之上才执行 land）。
  - 对任务结果判定应与实际风险相匹配：如果观测和返航目标全部完成，只是降落接口在“已着陆/不需要降落”状态下返回失败，应考虑区分“任务目标失败”和“结束动作异常”，避免过度将任务标记为整体失败。
  - 在没有重规划与应急停止触发的情况下发生失败，说明当前策略对异常缺乏分支处理。以后在关键步骤（起飞、降落、模式切换）失败时，应尝试至少一次高层级的恢复策略或安全检查，而不是直接结束任务。

- [2026-03-13 20:27] 无人机顺利起飞到约15米并飞抵目标坐标附近，成功在指定区域完成对地面亮红色发光物体的观测与标记。 -- 任务一次性成功且无重规划与应急停止，说明在 UAV 状态正常、环境无明显障碍时，现有起飞-飞行-感知-标记的技能链条稳定可靠，参数设定与执行顺序均合理。
  - 在 UAV 状态为 idle 且电量充足（约≥60%）的前提下，可直接执行连续的起飞-飞行-观测-标记技能链，无需中间插入额外状态检查，效率较高。
  - 高度约15米仍能有效完成地面亮色目标的识别和观测，对类似“地面明显颜色/发光目标”的搜索任务，可将 10–20 米高度作为默认工作区间之一。
  - 在无障碍物报告且无重规划触发的情况下，直线 fly_to 规划在中短距离场景（本次约 8 秒飞行）是安全且高效的，可作为同类任务的首选路径策略。

- [2026-03-13 20:27] 无人机顺利起飞到约15米并飞抵目标坐标附近，成功在指定区域完成对地面亮红色发光物体的观测与标记。 -- 任务一次性成功且无重规划与应急停止，说明在 UAV 状态正常、环境无明显障碍时，现有起飞-飞行-感知-标记的技能链条稳定可靠，参数设定与执行顺序均合理。
  - 在 UAV 状态为 idle 且电量充足（约≥60%）的前提下，可直接执行连续的起飞-飞行-观测-标记技能链，无需中间插入额外状态检查，效率较高。
  - 高度约15米仍能有效完成地面亮色目标的识别和观测，对类似“地面明显颜色/发光目标”的搜索任务，可将 10–20 米高度作为默认工作区间之一。
  - 在无障碍物报告且无重规划触发的情况下，直线 fly_to 规划在中短距离场景（本次约 8 秒飞行）是安全且高效的，可作为同类任务的首选路径策略。

- [2026-03-13 20:31] 任务在飞行与观察阶段均正常完成，但在返航后着陆阶段连续两次调用 land 技能失败，导致整体任务判定失败。 -- 核心失败原因集中在着陆阶段：前序 fly_to、observe、hover、return_to_launch 全部成功且耗时正常，说明飞控和导航链路工作良好；但 land 被连续调用两次且均为瞬时 FAIL（耗时=0.0s），高度疑似为状态或前置条件不满足导致的系统性失败，而非随机异常。由于没有应急停止、无障碍物事件，且任务结束时 UAV 状态为 idle、电量 100%，说明失败更可能是着陆技能的接口/状态机条件问题（例如当前状态不允许 land，或需要先切换模式/检查高度），而非环境危险或资源不足问题。
  - 对于任务链末尾的 land 等关键收尾动作，即使前序飞行和返航成功，也不能假定必然可用；需要在规划时预留 land 失败的应对策略（如重试次数上限、改用悬停等待或告警结束任务）。
  - 当技能执行结果为 FAIL 且耗时为 0.0s 时，往往意味着前置状态不满足或接口级拒绝，而不是物理过程中的失败；对于此类失败，在重复调用前应先检查并调整 UAV 状态（如飞行模式、高度、解锁状态），而不是盲目立即重试。
  - 返航后立即着陆存在耦合风险：如果 return_to_launch 将 UAV 置于某个特定模式或状态（例如已在地面、已切换为 idle），后续 land 可能被认为不适用；任务规划时应明确 RTH 后的期望状态，并根据状态选择是否需要再调用 land。
  - 任务执行统计中无重规划、无障碍事件且大部分时间消耗在 return_to_launch 上，说明在较长返航路径后，应在着陆前插入一次状态与高度检查（如通过 get_position 或状态查询），以减少不必要的 land 失败尝试。

- [2026-03-13 20:31] 任务在飞行与观察阶段均正常完成，但在返航后着陆阶段连续两次调用 land 技能失败，导致整体任务判定失败。 -- 核心失败原因集中在着陆阶段：前序 fly_to、observe、hover、return_to_launch 全部成功且耗时正常，说明飞控和导航链路工作良好；但 land 被连续调用两次且均为瞬时 FAIL（耗时=0.0s），高度疑似为状态或前置条件不满足导致的系统性失败，而非随机异常。由于没有应急停止、无障碍物事件，且任务结束时 UAV 状态为 idle、电量 100%，说明失败更可能是着陆技能的接口/状态机条件问题（例如当前状态不允许 land，或需要先切换模式/检查高度），而非环境危险或资源不足问题。
  - 对于任务链末尾的 land 等关键收尾动作，即使前序飞行和返航成功，也不能假定必然可用；需要在规划时预留 land 失败的应对策略（如重试次数上限、改用悬停等待或告警结束任务）。
  - 当技能执行结果为 FAIL 且耗时为 0.0s 时，往往意味着前置状态不满足或接口级拒绝，而不是物理过程中的失败；对于此类失败，在重复调用前应先检查并调整 UAV 状态（如飞行模式、高度、解锁状态），而不是盲目立即重试。
  - 返航后立即着陆存在耦合风险：如果 return_to_launch 将 UAV 置于某个特定模式或状态（例如已在地面、已切换为 idle），后续 land 可能被认为不适用；任务规划时应明确 RTH 后的期望状态，并根据状态选择是否需要再调用 land。
  - 任务执行统计中无重规划、无障碍事件且大部分时间消耗在 return_to_launch 上，说明在较长返航路径后，应在着陆前插入一次状态与高度检查（如通过 get_position 或状态查询），以减少不必要的 land 失败尝试。

- [2026-03-13 20:47] 无人机在正常状态下完成了标准的起飞、定点飞行、观察和返航降落闭环任务。 -- 任务成功的核心原因是 UAV 状态正常（status=idle、电量充足），规划链简单无重规划需求，且各技能按预期顺序执行且未遇到障碍或异常。
  - 在 UAV 状态为 idle 且电量充足时，可以直接执行标准的起飞→前往目标→观察→返航→降落链路，无需额外诊断或恢复步骤。
  - 对简单、无障碍环境的短距离任务，基础技能链（takeoff→fly_to→observe→fly_to→land）在默认参数下即可稳定完成，无需复杂规划或多次重规划。
  - 整体耗时中起飞和降落阶段占比很高（约 2/3 时间），规划类似任务时应预留主要时间给起降过程，而非水平飞行或观察。

- [2026-03-13 20:47] 无人机在正常状态下完成了标准的起飞、定点飞行、观察和返航降落闭环任务。 -- 任务成功的核心原因是 UAV 状态正常（status=idle、电量充足），规划链简单无重规划需求，且各技能按预期顺序执行且未遇到障碍或异常。
  - 在 UAV 状态为 idle 且电量充足时，可以直接执行标准的起飞→前往目标→观察→返航→降落链路，无需额外诊断或恢复步骤。
  - 对简单、无障碍环境的短距离任务，基础技能链（takeoff→fly_to→observe→fly_to→land）在默认参数下即可稳定完成，无需复杂规划或多次重规划。
  - 整体耗时中起飞和降落阶段占比很高（约 2/3 时间），规划类似任务时应预留主要时间给起降过程，而非水平飞行或观察。

- [2026-03-13 20:48] 无人机顺利起飞至约15米高度，飞抵指定坐标附近完成两次观察并成功标记亮红色发光物体位置及环境。 -- 任务一次规划顺利完成且无障碍、无应急情况，核心成功因素是状态正常（status=idle）、电量充足、直线飞行环境简单，以及技能链（起飞-飞行-观察-标记）配置合理且执行稳定。
  - 在无人机状态为 idle、电量高于约60%且无已知障碍信息时，可放心执行中等距离的单次飞行与观察任务，无需额外预留安全航点或中途检查。
  - 对“飞到固定坐标后向下观察并搜索特定颜色/光源目标”的任务模式，使用顺序链路：takeoff → fly_to(目标点上方) → 多次 observe → mark_location 是稳定可行的默认模板。
  - 对于静态目标搜索，如果第一次 observe 成功完成但存在识别不确定性，增加一次后续 observe 是有效且成本较低的冗余策略，本次两次观察在总时长上仍在可接受范围。
  - 在目标坐标附近完成任务时，将终态位置控制在目标周围约1–2米误差内（本次 ~1.8m）仍能保证地面观测与标记效果，对精度要求不极端时不必强行微调。
  - 在无障碍提示的场景中，不进行额外障碍侦测或重规划可以减少决策开销，对简单环境任务是一种可保留的轻量策略。

- [2026-03-13 20:48] 无人机顺利起飞至约15米高度，飞抵指定坐标附近完成两次观察并成功标记亮红色发光物体位置及环境。 -- 任务一次规划顺利完成且无障碍、无应急情况，核心成功因素是状态正常（status=idle）、电量充足、直线飞行环境简单，以及技能链（起飞-飞行-观察-标记）配置合理且执行稳定。
  - 在无人机状态为 idle、电量高于约60%且无已知障碍信息时，可放心执行中等距离的单次飞行与观察任务，无需额外预留安全航点或中途检查。
  - 对“飞到固定坐标后向下观察并搜索特定颜色/光源目标”的任务模式，使用顺序链路：takeoff → fly_to(目标点上方) → 多次 observe → mark_location 是稳定可行的默认模板。
  - 对于静态目标搜索，如果第一次 observe 成功完成但存在识别不确定性，增加一次后续 observe 是有效且成本较低的冗余策略，本次两次观察在总时长上仍在可接受范围。
  - 在目标坐标附近完成任务时，将终态位置控制在目标周围约1–2米误差内（本次 ~1.8m）仍能保证地面观测与标记效果，对精度要求不极端时不必强行微调。
  - 在无障碍提示的场景中，不进行额外障碍侦测或重规划可以减少决策开销，对简单环境任务是一种可保留的轻量策略。

- [2026-03-13 20:55] 任务在三个目标点的飞行与观察均完成，但在返航与降落阶段出现多次失败导致整体任务失败。 -- 核心失败原因在于返航(return_to_launch)和降落(land)阶段的异常：第一次返航耗时异常长且失败，之后多次 return_to_launch 与 land 调用呈现出短耗时/零耗时失败，说明在收尾阶段对飞行控制状态和位置状态缺乏检查与恢复逻辑，导致任务完成条件（安全返航并落地）未满足。
  - 任务是否成功不能只看中间子目标（如到点观察），必须显式把“成功返航并安全降落”作为硬性完成条件，并在规划和检查中突出这一点。
  - 当关键收尾技能（如 return_to_launch 或 land）第一次出现异常（超长耗时或 FAIL）时，应立即进行状态诊断（检查当前位置、高度、电量、状态标志），而不是盲目重复调用同一技能。
  - 零耗时或极短耗时的 FAIL 调用需要被视作“条件不满足/状态异常”的强信号，应触发分支处理（例如切换备用策略、重新获取位姿、检查控制权），而不是继续堆叠相同指令。
  - 返航失败后再次尝试前，应先通过 get_position 等技能确认当前位姿是否合理、与预期返航点是否相符，再决定是重新规划路径、改用手动降落，还是留在安全高度悬停等待。
  - 在任务设计中，应对长时耗但仍 FAIL 的调用设定超时与重试策略：超时后不要被动等待结束，而要主动中断并切换到诊断/安全策略。
  - 降落 land 连续失败时，不应继续零思考重试，应尝试先 return_to_launch 确保回到标准降落点，再次尝试降落；若仍失败，需进入“安全悬停+告警”流程。

- [2026-03-13 20:55] 任务在三个目标点的飞行与观察均完成，但在返航与降落阶段出现多次失败导致整体任务失败。 -- 核心失败原因在于返航(return_to_launch)和降落(land)阶段的异常：第一次返航耗时异常长且失败，之后多次 return_to_launch 与 land 调用呈现出短耗时/零耗时失败，说明在收尾阶段对飞行控制状态和位置状态缺乏检查与恢复逻辑，导致任务完成条件（安全返航并落地）未满足。
  - 任务是否成功不能只看中间子目标（如到点观察），必须显式把“成功返航并安全降落”作为硬性完成条件，并在规划和检查中突出这一点。
  - 当关键收尾技能（如 return_to_launch 或 land）第一次出现异常（超长耗时或 FAIL）时，应立即进行状态诊断（检查当前位置、高度、电量、状态标志），而不是盲目重复调用同一技能。
  - 零耗时或极短耗时的 FAIL 调用需要被视作“条件不满足/状态异常”的强信号，应触发分支处理（例如切换备用策略、重新获取位姿、检查控制权），而不是继续堆叠相同指令。
  - 返航失败后再次尝试前，应先通过 get_position 等技能确认当前位姿是否合理、与预期返航点是否相符，再决定是重新规划路径、改用手动降落，还是留在安全高度悬停等待。
  - 在任务设计中，应对长时耗但仍 FAIL 的调用设定超时与重试策略：超时后不要被动等待结束，而要主动中断并切换到诊断/安全策略。
  - 降落 land 连续失败时，不应继续零思考重试，应尝试先 return_to_launch 确保回到标准降落点，再次尝试降落；若仍失败，需进入“安全悬停+告警”流程。

- [2026-03-13 21:14] 无人机顺利完成三点观察任务但在返航阶段出现失败并进入error状态。 -- 核心任务（起飞、三点飞行与观察）全部按规划成功执行，过程无障碍、无重规划且耗时合理；但在最后的 return_to_launch 阶段发生失败，导致 UAV 状态变为 error，显示返航逻辑或环境条件存在潜在稳定性问题，需要独立于主要工作流程进行健壮性强化。
  - 即便任务的核心业务流程（观测/巡检）顺利完成，也需要单独评估和监控返航阶段的可靠性，将其作为任务成功判定的一部分，而不是简单假设返航一定成功。
  - 在规划多点观测任务时，分段 fly_to + hover + observe 的结构在无障碍环境中表现稳定，可优先采用；但需要预留时间和电量冗余给可能出现异常的返航阶段。
  - 当任务在最后阶段的关键技能（如 return_to_launch）失败且 UAV 状态变为 error 时，下次任务规划应首先检查并恢复 UAV 状态，而不是直接继续安排新的飞行任务。
  - 对于以区域观察为主的任务，如果返航阶段出现不稳定历史记录，可考虑在任务规划时增加中途健康检查（如在最后一个观察点后先执行状态/电量检查，再触发返航）。

- [2026-03-13 21:14] 无人机顺利完成三点观察任务但在返航阶段出现失败并进入error状态。 -- 核心任务（起飞、三点飞行与观察）全部按规划成功执行，过程无障碍、无重规划且耗时合理；但在最后的 return_to_launch 阶段发生失败，导致 UAV 状态变为 error，显示返航逻辑或环境条件存在潜在稳定性问题，需要独立于主要工作流程进行健壮性强化。
  - 即便任务的核心业务流程（观测/巡检）顺利完成，也需要单独评估和监控返航阶段的可靠性，将其作为任务成功判定的一部分，而不是简单假设返航一定成功。
  - 在规划多点观测任务时，分段 fly_to + hover + observe 的结构在无障碍环境中表现稳定，可优先采用；但需要预留时间和电量冗余给可能出现异常的返航阶段。
  - 当任务在最后阶段的关键技能（如 return_to_launch）失败且 UAV 状态变为 error 时，下次任务规划应首先检查并恢复 UAV 状态，而不是直接继续安排新的飞行任务。
  - 对于以区域观察为主的任务，如果返航阶段出现不稳定历史记录，可考虑在任务规划时增加中途健康检查（如在最后一个观察点后先执行状态/电量检查，再触发返航）。

- [2026-03-13 21:22] 无人机在成功升至指定高度后，执行向东飞行阶段失败，导致未能完成往返观察任务。 -- 任务在 change_altitude 成功后，卡在 fly_to 阶段并以失败结束，耗时集中在 fly_to 上；结合无障碍、无感知事件、UAV 状态为 idle 且电量充足，问题更可能源于飞行目标设定/导航逻辑（如目标点计算错误、方位/坐标系处理不当或超出可达区域）而非环境或资源不足。
  - 在没有历史统计和感知事件的前几次执行中，任务失败应优先怀疑目标设定与导航逻辑，而不是环境干扰，需要在规划层增加对目标点合理性的检查（如距离、相对方向与起飞点是否一致）。
  - 对“向东/向西”等相对方向类指令，应在规划阶段显式记录使用的坐标系和基准点（例如以起飞点为原点、以世界坐标 x 轴为东），避免方位解释不一致导致 fly_to 失败。
  - 当任务链条中前序技能（如 change_altitude）成功而后续飞行技能失败时，规划层应在失败时主动获取当前位置和状态进行事后验证，以判断是导航误差、目标设置错误还是控制层约束（如超出飞行范围）。
  - 在缺乏障碍和应急停止的情况下，fly_to 长时间执行后失败，应在未来加入超时前的中间状态检查（如每若干秒检查位置变化），判断无人机是否实际移动，从而及早重试或调整策略。

- [2026-03-13 21:22] 无人机在成功升至指定高度后，执行向东飞行阶段失败，导致未能完成往返观察任务。 -- 任务在 change_altitude 成功后，卡在 fly_to 阶段并以失败结束，耗时集中在 fly_to 上；结合无障碍、无感知事件、UAV 状态为 idle 且电量充足，问题更可能源于飞行目标设定/导航逻辑（如目标点计算错误、方位/坐标系处理不当或超出可达区域）而非环境或资源不足。
  - 在没有历史统计和感知事件的前几次执行中，任务失败应优先怀疑目标设定与导航逻辑，而不是环境干扰，需要在规划层增加对目标点合理性的检查（如距离、相对方向与起飞点是否一致）。
  - 对“向东/向西”等相对方向类指令，应在规划阶段显式记录使用的坐标系和基准点（例如以起飞点为原点、以世界坐标 x 轴为东），避免方位解释不一致导致 fly_to 失败。
  - 当任务链条中前序技能（如 change_altitude）成功而后续飞行技能失败时，规划层应在失败时主动获取当前位置和状态进行事后验证，以判断是导航误差、目标设置错误还是控制层约束（如超出飞行范围）。
  - 在缺乏障碍和应急停止的情况下，fly_to 长时间执行后失败，应在未来加入超时前的中间状态检查（如每若干秒检查位置变化），判断无人机是否实际移动，从而及早重试或调整策略。

- [2026-03-13 21:24] 无人机完成了起飞、定高、朝东飞行、观察并最终返回起飞点降落的任务，中途一次返航失败后自动复用 get_position 信息重试成功。 -- 任务整体成功，核心原因是各基础飞行与感知技能运行稳定，且在首次 return_to_launch 失败后，通过获取当前位置并再次调用返航技能完成了任务，体现了简单但有效的错误恢复机制。
  - 当返航类技能（return_to_launch）失败但无人机仍在空中且电量充足时，通过 get_position 获取当前位置信息后重试返航是一条有效的恢复路径。
  - 在无障碍、无重规划场景下，基础技能链（takeoff → change_altitude → fly_relative → observe → return_to_launch）可稳定执行，适合作为默认巡检/观察任务模板。
  - 在任务结束时仍需确认 UAV 状态（airborne/landed 等），即使任务逻辑上已经调用了返航，也要通过状态检查或位置校验确保真正完成降落。

- [2026-03-13 21:24] 无人机完成了起飞、定高、朝东飞行、观察并最终返回起飞点降落的任务，中途一次返航失败后自动复用 get_position 信息重试成功。 -- 任务整体成功，核心原因是各基础飞行与感知技能运行稳定，且在首次 return_to_launch 失败后，通过获取当前位置并再次调用返航技能完成了任务，体现了简单但有效的错误恢复机制。
  - 当返航类技能（return_to_launch）失败但无人机仍在空中且电量充足时，通过 get_position 获取当前位置信息后重试返航是一条有效的恢复路径。
  - 在无障碍、无重规划场景下，基础技能链（takeoff → change_altitude → fly_relative → observe → return_to_launch）可稳定执行，适合作为默认巡检/观察任务模板。
  - 在任务结束时仍需确认 UAV 状态（airborne/landed 等），即使任务逻辑上已经调用了返航，也要通过状态检查或位置校验确保真正完成降落。

- [2026-03-13 21:32] 任务在中途一次 fly_to 失败后仍继续局部搜索并完成观察，但未能准确到达目标坐标并报告目标物体位置，整体判定为失败。 -- 核心失败原因在于：首次 fly_to 到目标坐标失败后，缺乏明确的错误诊断与位置误差评估，后续通过多次 fly_relative 与 observe 进行局部搜索，但没有形成系统化的搜索策略与“未发现目标”的显式判定条件，最终既未精确到达[-30, 30, -15]附近，也未明确报告目标物体位置或确认其不存在，导致任务目标未满足。
  - 当关键导航技能（如 fly_to 目标点）失败时，应立刻调用 get_position 评估当前位置与目标点的误差，并根据误差大小选择重试飞到目标点或规划系统化搜索，而不是直接进入随意的相对位移搜索。
  - 对于包含“找到并报告某个目标物体位置”的任务，应在规划阶段明确：完成条件（检测到目标并报告坐标）、失败条件（在一定搜索范围和时间内未检测到目标时，明确报告“未发现”），避免无限或漫无目的的局部搜索。
  - 在搜索类任务中，使用 fly_relative 进行多次小步移动时应采用结构化模式（如网格/条带/螺旋搜索），并记录已搜索区域范围，而不是随机或主观位移，这样更有利于覆盖目标区域并支持后续“确认未发现”的合理性。
  - 首次导航失败后，如果电量和状态允许，应优先尝试一次或多次带容错参数的 fly_to 重试（例如降低速度、增大到达容差），而不是立即放弃绝对导航改用相对移动。
  - 对于有明确目标坐标的任务，任务结束前应再次调用 get_position 并在报告中包含“当前坐标与目标坐标的偏差”，即使没有找到目标物体，也能为后续分析和重试提供量化参考。
  - 在没有障碍物且飞行状态正常的情况下，fly_to 失败往往反映导航配置、目标高度/参考系或到达判定条件的问题，后续规划应主动检查这些因素，而不是仅把失败视为环境问题。

- [2026-03-13 21:32] 任务在中途一次 fly_to 失败后仍继续局部搜索并完成观察，但未能准确到达目标坐标并报告目标物体位置，整体判定为失败。 -- 核心失败原因在于：首次 fly_to 到目标坐标失败后，缺乏明确的错误诊断与位置误差评估，后续通过多次 fly_relative 与 observe 进行局部搜索，但没有形成系统化的搜索策略与“未发现目标”的显式判定条件，最终既未精确到达[-30, 30, -15]附近，也未明确报告目标物体位置或确认其不存在，导致任务目标未满足。
  - 当关键导航技能（如 fly_to 目标点）失败时，应立刻调用 get_position 评估当前位置与目标点的误差，并根据误差大小选择重试飞到目标点或规划系统化搜索，而不是直接进入随意的相对位移搜索。
  - 对于包含“找到并报告某个目标物体位置”的任务，应在规划阶段明确：完成条件（检测到目标并报告坐标）、失败条件（在一定搜索范围和时间内未检测到目标时，明确报告“未发现”），避免无限或漫无目的的局部搜索。
  - 在搜索类任务中，使用 fly_relative 进行多次小步移动时应采用结构化模式（如网格/条带/螺旋搜索），并记录已搜索区域范围，而不是随机或主观位移，这样更有利于覆盖目标区域并支持后续“确认未发现”的合理性。
  - 首次导航失败后，如果电量和状态允许，应优先尝试一次或多次带容错参数的 fly_to 重试（例如降低速度、增大到达容差），而不是立即放弃绝对导航改用相对移动。
  - 对于有明确目标坐标的任务，任务结束前应再次调用 get_position 并在报告中包含“当前坐标与目标坐标的偏差”，即使没有找到目标物体，也能为后续分析和重试提供量化参考。
  - 在没有障碍物且飞行状态正常的情况下，fly_to 失败往往反映导航配置、目标高度/参考系或到达判定条件的问题，后续规划应主动检查这些因素，而不是仅把失败视为环境问题。

- [2026-03-13 21:34] 任务在起飞阶段反复失败，未能开始对指定点位的观察飞行。 -- 核心失败原因是 UAV 当前 status=error 状态下仍多次尝试执行 takeoff，未进行任何故障确认或恢复，导致起飞技能持续失败，整个任务无法推进到航迹与观察阶段。
  - 在规划和执行任何飞行类任务前，应首先检查 UAV 状态字段（如 status），若为 error / fault / unknown，应中止飞行计划，转入诊断或恢复流程，而不是直接调用起飞。
  - 当首次 takeoff 失败且 UAV 状态未发生变化时，简单重试同一 takeoff 调用意义不大，应先收集诊断信息（状态、自检结果、错误码），再决定是尝试复位还是终止任务并上报。
  - 任务目标若依赖飞行（如巡航、观察点位），在高层规划阶段就应加入“可飞行性前置检查”步骤，将状态检查与电量检查统一到任务前期，而不是在失败后临时弥补。
  - 当前任务对 UAV 处于异常高度/位置（pos z≈0.64 但 status=error）的情况没有特殊处理，今后应避免根据位置或高度数值推断 UAV 可飞行性，而要以状态字段和技能成功率为准。

- [2026-03-13 21:34] 任务在起飞阶段反复失败，未能开始对指定点位的观察飞行。 -- 核心失败原因是 UAV 当前 status=error 状态下仍多次尝试执行 takeoff，未进行任何故障确认或恢复，导致起飞技能持续失败，整个任务无法推进到航迹与观察阶段。
  - 在规划和执行任何飞行类任务前，应首先检查 UAV 状态字段（如 status），若为 error / fault / unknown，应中止飞行计划，转入诊断或恢复流程，而不是直接调用起飞。
  - 当首次 takeoff 失败且 UAV 状态未发生变化时，简单重试同一 takeoff 调用意义不大，应先收集诊断信息（状态、自检结果、错误码），再决定是尝试复位还是终止任务并上报。
  - 任务目标若依赖飞行（如巡航、观察点位），在高层规划阶段就应加入“可飞行性前置检查”步骤，将状态检查与电量检查统一到任务前期，而不是在失败后临时弥补。
  - 当前任务对 UAV 处于异常高度/位置（pos z≈0.64 但 status=error）的情况没有特殊处理，今后应避免根据位置或高度数值推断 UAV 可飞行性，而要以状态字段和技能成功率为准。

- [2026-03-13 21:35] 任务在起飞阶段即失败，无人机未能开始执行任何观测点飞行与悬停动作。 -- 核心原因是 UAV 当前 status=error，在该状态下直接调用 takeoff 技能导致立即失败，未进行故障诊断或恢复，因此整个任务在第一步起飞阶段就终止。
  - 在规划包含飞行相关操作（takeoff、move 等）的任务前，应显式检查 UAV 状态；若 status=error，应先触发故障诊断或恢复流程，而不是直接执行飞行技能。
  - 即使 UAV 的 pos 和 battery 数据看起来正常，只要 status=error，就应将当前飞行链路视为不可用，优先执行安全与诊断步骤，避免无效尝试导致任务整体失败。
  - 对于以起飞为第一步的任务，可以在起飞前增加统一的健康检查步骤（状态、自检结果、通信链路），若检查不通过则中止任务并给出可恢复建议，而不是直接进入技能链。

- [2026-03-13 21:35] 任务在起飞阶段即失败，无人机未能开始执行任何观测点飞行与悬停动作。 -- 核心原因是 UAV 当前 status=error，在该状态下直接调用 takeoff 技能导致立即失败，未进行故障诊断或恢复，因此整个任务在第一步起飞阶段就终止。
  - 在规划包含飞行相关操作（takeoff、move 等）的任务前，应显式检查 UAV 状态；若 status=error，应先触发故障诊断或恢复流程，而不是直接执行飞行技能。
  - 即使 UAV 的 pos 和 battery 数据看起来正常，只要 status=error，就应将当前飞行链路视为不可用，优先执行安全与诊断步骤，避免无效尝试导致任务整体失败。
  - 对于以起飞为第一步的任务，可以在起飞前增加统一的健康检查步骤（状态、自检结果、通信链路），若检查不通过则中止任务并给出可恢复建议，而不是直接进入技能链。

- [2026-03-14 16:12] 无人机顺利完成起飞、定向飞行与观察，但自动返航多次失败，最终通过直接降落完成任务。 -- 任务整体成功的关键在于基础飞行与观察技能执行稳定；返航失败未导致任务失败，是因为规划层在多次 return_to_launch 失败后选择了安全的降落策略，从而在返航模块异常或条件不满足的情况下仍保证了任务闭环与安全着陆。
  - 在没有历史统计和已知风险的情况下，基础动作链（调整高度→定向飞行→观察）可优先使用，表现稳定可靠。
  - return_to_launch 连续失败时，应有明确的重试次数上限与兜底方案（如改为直接在当前位置降落或尝试手动规划返航路径），避免长时间卡在同一技能上浪费时间与电量。
  - 任务规划中应将“安全终止方案”（例如原地降落）视为独立步骤，而不是完全依赖 return_to_launch，以对冲自动返航的潜在不可靠性。
  - 当自动返航耗时接近或超过单次预期时间但仍失败时，需要提前触发状态检测（如 get_position、姿态与控制模式检查），而不是仅仅重复调用同一技能。
  - 在没有障碍和感知事件的简单场景中，如果返航模块表现异常，可以考虑短期内减少对其的依赖，优先采用简单可验证的移动+降落组合来保证任务完成。

- [2026-03-14 16:12] 无人机顺利完成起飞、定向飞行与观察，但自动返航多次失败，最终通过直接降落完成任务。 -- 任务整体成功的关键在于基础飞行与观察技能执行稳定；返航失败未导致任务失败，是因为规划层在多次 return_to_launch 失败后选择了安全的降落策略，从而在返航模块异常或条件不满足的情况下仍保证了任务闭环与安全着陆。
  - 在没有历史统计和已知风险的情况下，基础动作链（调整高度→定向飞行→观察）可优先使用，表现稳定可靠。
  - return_to_launch 连续失败时，应有明确的重试次数上限与兜底方案（如改为直接在当前位置降落或尝试手动规划返航路径），避免长时间卡在同一技能上浪费时间与电量。
  - 任务规划中应将“安全终止方案”（例如原地降落）视为独立步骤，而不是完全依赖 return_to_launch，以对冲自动返航的潜在不可靠性。
  - 当自动返航耗时接近或超过单次预期时间但仍失败时，需要提前触发状态检测（如 get_position、姿态与控制模式检查），而不是仅仅重复调用同一技能。
  - 在没有障碍和感知事件的简单场景中，如果返航模块表现异常，可以考虑短期内减少对其的依赖，优先采用简单可验证的移动+降落组合来保证任务完成。

- [2026-03-14 16:14] 无人机顺利起飞至约15米高度，飞抵目标坐标附近完成两次观察并成功标记亮红色发光物体位置。 -- 任务一次性规划完成且过程中无障碍、无状态异常，所有关键技能均按预期执行且时长稳定，是标准条件下的顺利成功案例。
  - 在无人机状态正常且无障碍的环境下，简单顺序链式流程（takeoff → fly_to → observe → mark_location）即可可靠完成侦察类任务，无需复杂重规划。
  - 在搜索类任务中，进行至少两次 observe 有助于提高目标确认的可信度，且当前时耗（约7–8秒级）在可接受范围内。
  - 目标点附近存在少量位置偏差是可接受的（示例中到达[-30,40,-15]附近落在[-30.92,41.38,14.9]），说明飞行控制在当前精度下足以支撑视觉搜索任务。

- [2026-03-14 16:14] 无人机顺利起飞至约15米高度，飞抵目标坐标附近完成两次观察并成功标记亮红色发光物体位置。 -- 任务一次性规划完成且过程中无障碍、无状态异常，所有关键技能均按预期执行且时长稳定，是标准条件下的顺利成功案例。
  - 在无人机状态正常且无障碍的环境下，简单顺序链式流程（takeoff → fly_to → observe → mark_location）即可可靠完成侦察类任务，无需复杂重规划。
  - 在搜索类任务中，进行至少两次 observe 有助于提高目标确认的可信度，且当前时耗（约7–8秒级）在可接受范围内。
  - 目标点附近存在少量位置偏差是可接受的（示例中到达[-30,40,-15]附近落在[-30.92,41.38,14.9]），说明飞行控制在当前精度下足以支撑视觉搜索任务。

- [2026-03-14 16:17] 无人机按序完成三点观测任务，但在返航阶段 return_to_launch 失败，最终通过 land 成功结束任务。 -- 任务主目标（按给定三个 NED 点飞行并观察）全部成功，说明路径规划与飞行控制稳定；return_to_launch 超时失败表明自动返航存在鲁棒性问题或触发条件不完善，但因后续 land 成功，整体归因于返航逻辑或条件配置不足而非飞行能力欠缺。
  - 当任务主流程已完成而返航动作出现失败时，应优先保障安全结束：可以在 return_to_launch 超时或失败后自动降级为就地或安全位置的 land，而不是继续尝试复杂动作。
  - 在任务规划中可将返航视为可替代步骤：只要起飞点附近没有额外安全约束，land 作为兜底手段足以完成任务终止，return_to_launch 可作为优化而非唯一正确结束方式。
  - 首次执行某类巡视任务时，如未出现障碍和重规划，可将当前飞行时间与动作序列作为后续类似任务的参考基线，用于后续超时阈值和安全冗余设计。

- [2026-03-14 16:17] 无人机按序完成三点观测任务，但在返航阶段 return_to_launch 失败，最终通过 land 成功结束任务。 -- 任务主目标（按给定三个 NED 点飞行并观察）全部成功，说明路径规划与飞行控制稳定；return_to_launch 超时失败表明自动返航存在鲁棒性问题或触发条件不完善，但因后续 land 成功，整体归因于返航逻辑或条件配置不足而非飞行能力欠缺。
  - 当任务主流程已完成而返航动作出现失败时，应优先保障安全结束：可以在 return_to_launch 超时或失败后自动降级为就地或安全位置的 land，而不是继续尝试复杂动作。
  - 在任务规划中可将返航视为可替代步骤：只要起飞点附近没有额外安全约束，land 作为兜底手段足以完成任务终止，return_to_launch 可作为优化而非唯一正确结束方式。
  - 首次执行某类巡视任务时，如未出现障碍和重规划，可将当前飞行时间与动作序列作为后续类似任务的参考基线，用于后续超时阈值和安全冗余设计。

- [2026-03-14 16:38] 无人机完成起飞、爬升、东向飞行和观察，但在返回起飞点阶段技能报告失败，任务整体仍视为成功结束。 -- 从技能链看，核心飞行与观察动作均顺利完成，返回阶段 return_to_launch 被判定为 FAIL 且耗时达到上限（30s），结合最终世界状态为 status=error，推测在返航过程中出现系统级错误或状态异常导致技能失败，但飞行器仍处于空中且电量充足；任务被标记成功可能基于主要目标（起飞、飞行、观察）完成，而非严格依赖自动返航技能的成功标记。
  - 不能仅以任务最终标签“成功”判断所有关键技能都可靠完成，需要结合技能级结果和结束时 UAV 状态进行复盘，尤其关注 FAIL 的关键技能。
  - 返航类技能（return_to_launch）即使耗时较长也可能最终未完成且将 UAV 留在空中，应在规划中预留备选收尾机制（如手动 fly_to 起飞点+降落），而不是单点依赖自动返航。
  - 当任务链末尾技能失败且 UAV status=error 时，后续任务不应直接衔接复杂飞行规划，需要先安排诊断或复位类技能（如状态检查、故障恢复）。
  - 在没有障碍和重规划的简单任务中，如果单个关键技能耗时明显长于其他步骤且以 FAIL 结束，应作为重点排查对象，在后续任务中做更保守的使用或增加冗余手段。

- [2026-03-14 16:38] 无人机完成起飞、爬升、东向飞行和观察，但在返回起飞点阶段技能报告失败，任务整体仍视为成功结束。 -- 从技能链看，核心飞行与观察动作均顺利完成，返回阶段 return_to_launch 被判定为 FAIL 且耗时达到上限（30s），结合最终世界状态为 status=error，推测在返航过程中出现系统级错误或状态异常导致技能失败，但飞行器仍处于空中且电量充足；任务被标记成功可能基于主要目标（起飞、飞行、观察）完成，而非严格依赖自动返航技能的成功标记。
  - 不能仅以任务最终标签“成功”判断所有关键技能都可靠完成，需要结合技能级结果和结束时 UAV 状态进行复盘，尤其关注 FAIL 的关键技能。
  - 返航类技能（return_to_launch）即使耗时较长也可能最终未完成且将 UAV 留在空中，应在规划中预留备选收尾机制（如手动 fly_to 起飞点+降落），而不是单点依赖自动返航。
  - 当任务链末尾技能失败且 UAV status=error 时，后续任务不应直接衔接复杂飞行规划，需要先安排诊断或复位类技能（如状态检查、故障恢复）。
  - 在没有障碍和重规划的简单任务中，如果单个关键技能耗时明显长于其他步骤且以 FAIL 结束，应作为重点排查对象，在后续任务中做更保守的使用或增加冗余手段。

- [2026-03-14 16:40] 任务在完成首个观察点后因重复触发无意义的 takeoff 技能而标记为失败。 -- 从技能链看，首次 takeoff、飞行、悬停和观察均成功，UAV 最终状态为 idle 且在目标区域上空，说明核心飞行目标基本达成；任务被判定失败的直接原因是后续两次 takeoff 在飞行已结束且状态为 idle 时被调用，立即失败且耗时为 0.0s，暴露出任务分解/编排逻辑在多阶段任务中对 UAV 当前飞行状态缺乏检查，导致冗余且必然失败的起飞指令。
  - 在多点序列任务中，要显式区分“地面起飞”与“空中位移/悬停”阶段，避免在 UAV 已在空中且 status=idle 时再次调用 takeoff。
  - 任务成功判定应优先绑定到关键目标（例如是否依次在指定三点完成悬停与观察），而不是对所有技能调用是否全为 [OK] 做硬性约束；非关键技能的可预期失败不应直接导致任务整体失败。
  - 在规划后续技能前，应根据当前 UAV 状态（如 status、altitude）进行前置条件检查，必要时通过 get_status / get_position 等技能确认再决定是否需要执行 takeoff。
  - 对连续多点观察任务，更稳健的流程是：一次 takeoff 拉到作业高度，然后以 fly_to+hover+observe 组合在多个点间巡航，无需多次起飞降落，减少冗余与失败机会。

- [2026-03-14 16:40] 任务在完成首个观察点后因重复触发无意义的 takeoff 技能而标记为失败。 -- 从技能链看，首次 takeoff、飞行、悬停和观察均成功，UAV 最终状态为 idle 且在目标区域上空，说明核心飞行目标基本达成；任务被判定失败的直接原因是后续两次 takeoff 在飞行已结束且状态为 idle 时被调用，立即失败且耗时为 0.0s，暴露出任务分解/编排逻辑在多阶段任务中对 UAV 当前飞行状态缺乏检查，导致冗余且必然失败的起飞指令。
  - 在多点序列任务中，要显式区分“地面起飞”与“空中位移/悬停”阶段，避免在 UAV 已在空中且 status=idle 时再次调用 takeoff。
  - 任务成功判定应优先绑定到关键目标（例如是否依次在指定三点完成悬停与观察），而不是对所有技能调用是否全为 [OK] 做硬性约束；非关键技能的可预期失败不应直接导致任务整体失败。
  - 在规划后续技能前，应根据当前 UAV 状态（如 status、altitude）进行前置条件检查，必要时通过 get_status / get_position 等技能确认再决定是否需要执行 takeoff。
  - 对连续多点观察任务，更稳健的流程是：一次 takeoff 拉到作业高度，然后以 fly_to+hover+observe 组合在多个点间巡航，无需多次起飞降落，减少冗余与失败机会。

- [2026-03-18 13:27] 无人机在初始状态正常（idle、电量充足）的情况下，多次基础技能调用失败，任务在起飞前即告失败。 -- 关键前置技能 get_position 首次调用失败，随后多次 takeoff 调用均立即失败，且无重规划或错误恢复步骤；任务失败的核心原因是对基础状态/飞行技能失败缺乏诊断与恢复策略，而非电量或环境限制。
  - 当第一个基础状态技能（如 get_position）失败且耗时异常短或为0时，应立即进入故障诊断分支，而不是继续按原计划顺序调用其它飞行技能。
  - 在起飞前必须建立明确的前置条件检查链：status ∈ {idle, ready} 且 get_position 成功 且 get_battery 正常 且无 error 标志，否则不应尝试 takeoff。
  - 连续多次调用同一个已失败的飞行技能（如连续 takeoff 失败）意义有限，应限制重试次数（例如1–2次），之后转为错误处理（停飞、报警、尝试 reset）。
  - 在没有任何感知事件和障碍的情况下，如果技能仍然快速失败，应优先考虑系统状态异常或接口/模拟层问题，而不是环境问题。
  - 首次执行在没有历史统计数据的前提下，更应显式记录失败模式（比如“基础状态技能立即失败”），为后续任务提供早期诊断依据。

- [2026-03-18 13:27] 无人机在初始状态正常（idle、电量充足）的情况下，多次基础技能调用失败，任务在起飞前即告失败。 -- 关键前置技能 get_position 首次调用失败，随后多次 takeoff 调用均立即失败，且无重规划或错误恢复步骤；任务失败的核心原因是对基础状态/飞行技能失败缺乏诊断与恢复策略，而非电量或环境限制。
  - 当第一个基础状态技能（如 get_position）失败且耗时异常短或为0时，应立即进入故障诊断分支，而不是继续按原计划顺序调用其它飞行技能。
  - 在起飞前必须建立明确的前置条件检查链：status ∈ {idle, ready} 且 get_position 成功 且 get_battery 正常 且无 error 标志，否则不应尝试 takeoff。
  - 连续多次调用同一个已失败的飞行技能（如连续 takeoff 失败）意义有限，应限制重试次数（例如1–2次），之后转为错误处理（停飞、报警、尝试 reset）。
  - 在没有任何感知事件和障碍的情况下，如果技能仍然快速失败，应优先考虑系统状态异常或接口/模拟层问题，而不是环境问题。
  - 首次执行在没有历史统计数据的前提下，更应显式记录失败模式（比如“基础状态技能立即失败”），为后续任务提供早期诊断依据。

- [2026-03-18 15:13] 任务在无技能调用和无外部干扰的情况下被标记为成功结束。 -- 成功主要源于任务定义宽松——起飞前状态已为 airborne，且无需额外动作或技能执行即可满足任务完成条件，而非通过实际操作产生状态变化。
  - 当任务结果为成功且无任何技能调用时，需要警惕任务定义是否与实际意图匹配（例如“起飞”任务在 UAV 已经 airborne 时如何判定目标达成）。
  - 在规划类似“起飞”这类状态型任务时，应显式检查初始状态：若 UAV 已为 airborne，可将任务视为“已满足”的快速路径，但最好记录一次状态确认步骤以便日后追踪。
  - 任务耗时接近 0 且无感知/技能记录时，可能意味着只是状态检查通过而非真实执行，这类记录在长期经验中价值有限，应与包含动作链的记录区分看待。

- [2026-03-18 15:13] 任务在无技能调用和无外部干扰的情况下被标记为成功结束。 -- 成功主要源于任务定义宽松——起飞前状态已为 airborne，且无需额外动作或技能执行即可满足任务完成条件，而非通过实际操作产生状态变化。
  - 当任务结果为成功且无任何技能调用时，需要警惕任务定义是否与实际意图匹配（例如“起飞”任务在 UAV 已经 airborne 时如何判定目标达成）。
  - 在规划类似“起飞”这类状态型任务时，应显式检查初始状态：若 UAV 已为 airborne，可将任务视为“已满足”的快速路径，但最好记录一次状态确认步骤以便日后追踪。
  - 任务耗时接近 0 且无感知/技能记录时，可能意味着只是状态检查通过而非真实执行，这类记录在长期经验中价值有限，应与包含动作链的记录区分看待。

- [2026-03-18 15:14] 任务“起飞”在无技能调用、无重规划和无感知事件的情况下被直接判定为成功。 -- 成功原因主要是任务在逻辑层面被标记为完成，而非通过真实技能链执行；当前世界状态显示 UAV 已处于 airborne 状态，因而系统可能直接将起飞任务视为已达成目标。
  - 当任务目标状态（如 UAV status=airborne）在开始时已经满足时，可以将任务视为完成，但应明确区分“目标已满足”与“通过技能执行达成目标”两种成功类型。
  - 对于飞行相关任务，如果没有任何技能链和感知事件，且耗时为 0s，需要在上层逻辑中确认是否是因为起始世界状态已经满足目标，以避免误判规划或执行异常为成功。
  - 在记录任务经验时，即便没有技能调用，也应尽量标注“基于初始状态达成目标”的成功原因，便于后续分析成功是否依赖真实控制过程。

- [2026-03-18 15:14] 任务“起飞”在无技能调用、无重规划和无感知事件的情况下被直接判定为成功。 -- 成功原因主要是任务在逻辑层面被标记为完成，而非通过真实技能链执行；当前世界状态显示 UAV 已处于 airborne 状态，因而系统可能直接将起飞任务视为已达成目标。
  - 当任务目标状态（如 UAV status=airborne）在开始时已经满足时，可以将任务视为完成，但应明确区分“目标已满足”与“通过技能执行达成目标”两种成功类型。
  - 对于飞行相关任务，如果没有任何技能链和感知事件，且耗时为 0s，需要在上层逻辑中确认是否是因为起始世界状态已经满足目标，以避免误判规划或执行异常为成功。
  - 在记录任务经验时，即便没有技能调用，也应尽量标注“基于初始状态达成目标”的成功原因，便于后续分析成功是否依赖真实控制过程。

- [2026-03-18 22:46] 起飞任务在 UAV 处于 error 状态下执行，导致起飞与状态获取技能连续失败。 -- 失败的核心原因是任务在 UAV_1 status=error 的前提下直接调用飞行与状态技能，未进行任何故障检测或恢复，导致第一次起飞虽耗时较长但未成功，后续 takeoff 和 get_position 立即失败。
  - 在任何飞行或感知类任务开始前，应首先检查 UAV 的 status 字段；若为 error，则应中止当前飞行计划并尝试执行诊断或恢复流程，而不是直接调用 takeoff、get_position 等技能。
  - 当同一个技能在短时间内出现一次长耗时执行后紧接一次 0.0s 失败时，应判定为底层系统存在异常，而不是简单重试同一技能；需要插入状态检查或切换到故障处理子任务。
  - 任务规划中应将 UAV 状态检查（如 get_health / check_status 类技能）作为飞行任务的前置条件，而不是仅依赖当前位置和电量等数值是否合理。
  - 在无任何感知事件记录、且 UAV 状态为 error 的情况下发生失败，应优先怀疑系统级故障而非环境障碍，避免误导后续规划。

- [2026-03-18 22:46] 起飞任务在 UAV 处于 error 状态下执行，导致起飞与状态获取技能连续失败。 -- 失败的核心原因是任务在 UAV_1 status=error 的前提下直接调用飞行与状态技能，未进行任何故障检测或恢复，导致第一次起飞虽耗时较长但未成功，后续 takeoff 和 get_position 立即失败。
  - 在任何飞行或感知类任务开始前，应首先检查 UAV 的 status 字段；若为 error，则应中止当前飞行计划并尝试执行诊断或恢复流程，而不是直接调用 takeoff、get_position 等技能。
  - 当同一个技能在短时间内出现一次长耗时执行后紧接一次 0.0s 失败时，应判定为底层系统存在异常，而不是简单重试同一技能；需要插入状态检查或切换到故障处理子任务。
  - 任务规划中应将 UAV 状态检查（如 get_health / check_status 类技能）作为飞行任务的前置条件，而不是仅依赖当前位置和电量等数值是否合理。
  - 在无任何感知事件记录、且 UAV 状态为 error 的情况下发生失败，应优先怀疑系统级故障而非环境障碍，避免误导后续规划。

- [2026-03-23 20:24] 任务失败，观察技能未能执行。 -- 失败的核心原因是无人机状态为error，导致观察技能无法执行。
  - 在任务规划中，应优先检查无人机的状态，确保其处于正常状态后再执行观察类技能。
  - 即使飞行和位置技能成功执行，仍需注意无人机的状态可能影响后续技能的执行。

- [2026-03-23 20:24] 任务失败，观察技能未能执行。 -- 失败的核心原因是无人机状态为error，导致观察技能无法执行。
  - 在任务规划中，应优先检查无人机的状态，确保其处于正常状态后再执行观察类技能。
  - 即使飞行和位置技能成功执行，仍需注意无人机的状态可能影响后续技能的执行。

- [2026-03-23 20:29] 任务失败，起飞和飞行成功，但观察技能执行失败。 -- 观察技能失败的核心原因可能与无人机状态为error有关，尽管位置和电量正常，但状态错误可能导致感知链路不可用。
  - 在任务规划中，必须在执行观察或其他感知技能前确认无人机状态正常。
  - 即使无人机位置和电量显示正常，状态为error时需要优先进行故障排查或恢复。

- [2026-03-23 20:29] 任务失败，起飞和飞行成功，但观察技能执行失败。 -- 观察技能失败的核心原因可能与无人机状态为error有关，尽管位置和电量正常，但状态错误可能导致感知链路不可用。
  - 在任务规划中，必须在执行观察或其他感知技能前确认无人机状态正常。
  - 即使无人机位置和电量显示正常，状态为error时需要优先进行故障排查或恢复。

- [2026-03-23 20:41] 任务失败，观察技能未能成功执行。 -- 任务失败的核心原因是无人机在执行观察技能时状态为error，导致观察技能失败。
  - 在任务规划中，应在每个关键步骤前检查无人机的状态，以确保其处于正常工作状态。
  - 即使无人机的电量充足和位置正常，状态为error时仍需优先处理故障确认或恢复。

- [2026-03-23 20:41] 任务失败，观察技能未能成功执行。 -- 任务失败的核心原因是无人机在执行观察技能时状态为error，导致观察技能失败。
  - 在任务规划中，应在每个关键步骤前检查无人机的状态，以确保其处于正常工作状态。
  - 即使无人机的电量充足和位置正常，状态为error时仍需优先处理故障确认或恢复。

- [2026-03-23 21:06] 任务失败，因降落技能未成功执行。 -- 任务失败的核心原因是降落技能执行失败，尽管之前的飞行和观察阶段均成功完成。
  - 即使任务中大部分技能执行成功，关键技能的失败仍会导致整体任务失败，因此需要特别关注关键技能的可靠性。
  - 在任务执行前，应确保无人机的状态正常，避免在状态为error时执行任务。

- [2026-03-23 21:06] 任务失败，因降落技能未成功执行。 -- 任务失败的核心原因是降落技能执行失败，尽管之前的飞行和观察阶段均成功完成。
  - 即使任务中大部分技能执行成功，关键技能的失败仍会导致整体任务失败，因此需要特别关注关键技能的可靠性。
  - 在任务执行前，应确保无人机的状态正常，避免在状态为error时执行任务。

- [2026-03-23 21:17] 任务成功地完成了无人机的起飞、飞行、观察和返回降落操作。 -- 任务成功的核心原因是所有技能链顺利执行，没有遇到任何障碍或异常情况。
  - 任务规划和执行过程中没有遇到任何障碍或异常，表明在当前环境和条件下，规划的路径和高度是安全的。
  - 无人机的电池状态为100%且状态为idle，确保了任务执行的顺利进行。保持良好的电池状态对于任务成功至关重要。

- [2026-03-23 21:17] 任务成功地完成了无人机的起飞、飞行、观察和返回降落操作。 -- 任务成功的核心原因是所有技能链顺利执行，没有遇到任何障碍或异常情况。
  - 任务规划和执行过程中没有遇到任何障碍或异常，表明在当前环境和条件下，规划的路径和高度是安全的。
  - 无人机的电池状态为100%且状态为idle，确保了任务执行的顺利进行。保持良好的电池状态对于任务成功至关重要。

- [2026-03-23 21:52] 任务成功地完成了无人机的起飞、飞行、观察和降落操作。 -- 任务成功的核心原因是所有技能都按计划执行，没有遇到障碍或需要重新规划。
  - 在没有障碍物和状态正常的情况下，任务可以顺利完成，表明当前的技能链和规划策略是有效的。
  - 观察阶段耗时最长，未来任务规划中应考虑观察任务的时间占比，以优化整体任务时间。

- [2026-03-23 21:52] 任务成功地完成了无人机的起飞、飞行、观察和降落操作。 -- 任务成功的核心原因是所有技能都按计划执行，没有遇到障碍或需要重新规划。
  - 在没有障碍物和状态正常的情况下，任务可以顺利完成，表明当前的技能链和规划策略是有效的。
  - 观察阶段耗时最长，未来任务规划中应考虑观察任务的时间占比，以优化整体任务时间。

- [2026-03-23 23:11] 任务失败，原因是报告技能未能执行。 -- 任务失败的核心原因是报告技能未能执行，可能与无人机的状态错误有关。
  - 在任务规划中，应确保所有关键技能的执行条件都满足，尤其是报告类技能需要确认无人机状态正常。
  - 在任务执行前，应对无人机的状态进行全面检查，以避免因状态错误导致的任务失败。

- [2026-03-23 23:11] 任务失败，原因是报告技能未能执行。 -- 任务失败的核心原因是报告技能未能执行，可能与无人机的状态错误有关。
  - 在任务规划中，应确保所有关键技能的执行条件都满足，尤其是报告类技能需要确认无人机状态正常。
  - 在任务执行前，应对无人机的状态进行全面检查，以避免因状态错误导致的任务失败。

- [2026-03-24 00:23] 无人机成功起飞并巡航至四个地标，任务顺利完成。 -- 任务成功的核心原因是技能执行链的稳定性和无障碍的飞行环境。
  - 在无障碍环境中，技能链可以顺利执行，减少了重规划和应急停止的需求。
  - 飞行路径规划合理，耗时分布均匀，未出现明显的时间瓶颈。

- [2026-03-24 00:23] 无人机成功起飞并巡航至四个地标，任务顺利完成。 -- 任务成功的核心原因是技能执行链的稳定性和无障碍的飞行环境。
  - 在无障碍环境中，技能链可以顺利执行，减少了重规划和应急停止的需求。
  - 飞行路径规划合理，耗时分布均匀，未出现明显的时间瓶颈。

- [2026-03-24 01:28] 无人机成功执行起飞、巡航三个地标并返回的任务。 -- 任务成功的核心原因是所有技能顺利执行，没有遇到任何障碍或需要重规划。
  - 在没有障碍和紧急情况的环境中，任务规划可以保持简单直接，减少复杂性以提高成功率。
  - 飞行任务中，合理的技能链执行顺序（如先起飞再巡航）是确保任务成功的关键。

- [2026-03-24 01:28] 无人机成功执行起飞、巡航三个地标并返回的任务。 -- 任务成功的核心原因是所有技能顺利执行，没有遇到任何障碍或需要重规划。
  - 在没有障碍和紧急情况的环境中，任务规划可以保持简单直接，减少复杂性以提高成功率。
  - 飞行任务中，合理的技能链执行顺序（如先起飞再巡航）是确保任务成功的关键。

- [2026-03-28 00:04] 成功完成四点巡检任务，依次飞往[-0,0],[0,-15],[-30,-17],[-33,16]并在每个位置执行观察与报告，全程无障碍无重规划。 -- 任务完全成功。无人机电量充足（100%），状态正常（idle），规划路径合理，技能链执行顺序正确（fly_to → observe → report 循环），无任何异常事件干扰，是一次标准的多点巡检范例执行。
  - 多点巡检任务的标准执行模式为：takeoff → [fly_to → observe → report] × N → fly_to(返回/结束)，该模式在本次任务中验证有效。
  - 巡检任务中每个目标点均应紧跟 observe + report，确保感知数据及时记录，不应将多个 fly_to 连续执行后再统一观察。
  - takeoff 耗时约23.7s，在任务时间预算中占比较大（约30%），规划总时长时需充分考虑起飞时间。
  - 四点巡检（含起飞）总耗时约78.6s，平均每个巡检点（fly_to+observe+report）约耗时11~14s，可作为同类任务时间估算基准。

- [2026-03-28 00:04] 成功完成四点巡检任务，依次飞往[-0,0],[0,-15],[-30,-17],[-33,16]并在每个位置执行观察与报告，全程无障碍无重规划。 -- 任务完全成功。无人机电量充足（100%），状态正常（idle），规划路径合理，技能链执行顺序正确（fly_to → observe → report 循环），无任何异常事件干扰，是一次标准的多点巡检范例执行。
  - 多点巡检任务的标准执行模式为：takeoff → [fly_to → observe → report] × N → fly_to(返回/结束)，该模式在本次任务中验证有效。
  - 巡检任务中每个目标点均应紧跟 observe + report，确保感知数据及时记录，不应将多个 fly_to 连续执行后再统一观察。
  - takeoff 耗时约23.7s，在任务时间预算中占比较大（约30%），规划总时长时需充分考虑起飞时间。
  - 四点巡检（含起飞）总耗时约78.6s，平均每个巡检点（fly_to+observe+report）约耗时11~14s，可作为同类任务时间估算基准。

## 策略偏好

- [2026-03-13 15:02] 在涉及视觉/环境描述的任务中，默认采用“多层感知链”：先使用 look_around 获取全局概况，再调用 scan_area 细化区域信息，最后用多次冗余 observe 获取可描述细节；规划时引入连续失败监控（尤其是 observe），当同一技能连续失败达到阈值时，自动切换到 hover + 替代感知技能的保守策略。同时，将 UAV 最终状态（例如 error 标记）作为任务风险信号，即使任务结果为成功，也应在后续任务中更谨慎地使用高负载感知技能。


- [2026-03-13 15:02] 在涉及视觉/环境描述的任务中，默认采用“多层感知链”：先使用 look_around 获取全局概况，再调用 scan_area 细化区域信息，最后用多次冗余 observe 获取可描述细节；规划时引入连续失败监控（尤其是 observe），当同一技能连续失败达到阈值时，自动切换到 hover + 替代感知技能的保守策略。同时，将 UAV 最终状态（例如 error 标记）作为任务风险信号，即使任务结果为成功，也应在后续任务中更谨慎地使用高负载感知技能。

- [2026-03-13 15:23] 在所有涉及 UAV 的任务规划中增加统一的前置步骤：首先调用状态/健康检查技能（或读取 status 字段）；若状态为 error/critical，则优先进入“诊断-恢复”子流程（如尝试 reset、自检、报告无法执行），禁止直接调用飞行与导航技能。只有在状态恢复为正常后，才继续执行 takeoff、get_position 等后续动作。未来遇到 0.0s 内即失败的技能调用，应优先排查系统状态或权限问题，而不是盲目调整任务参数。

- [2026-03-13 15:23] 在所有涉及 UAV 的任务规划中增加统一的前置步骤：首先调用状态/健康检查技能（或读取 status 字段）；若状态为 error/critical，则优先进入“诊断-恢复”子流程（如尝试 reset、自检、报告无法执行），禁止直接调用飞行与导航技能。只有在状态恢复为正常后，才继续执行 takeoff、get_position 等后续动作。未来遇到 0.0s 内即失败的技能调用，应优先排查系统状态或权限问题，而不是盲目调整任务参数。

- [2026-03-13 15:27] 在 UAV 状态为 idle 且电量充足的常规任务中，可以将“takeoff → change_altitude → observe”作为默认侦察任务模板；同时在时间规划上预留更多给 observe 环节，将其视为主耗时步骤。

- [2026-03-13 15:27] 在 UAV 状态为 idle 且电量充足的常规任务中，可以将“takeoff → change_altitude → observe”作为默认侦察任务模板；同时在时间规划上预留更多给 observe 环节，将其视为主耗时步骤。

- [2026-03-13 15:36] 在后续“搜索并救援”类任务中，应采用分阶段闭环策略：1）先用 look_around 或系统化 fly_relative 路径建立基础覆盖；2）每次 observe 后必须更新内部状态（是否发现候选目标、位置置信度）；3）仅在目标已确认时才调用 rescue_person，且在 rescue_person 失败或 observe 多次失败时触发重规划（调整搜索模式或区域）；优先避免在缺乏目标信息的情况下直接调用救援技能。

- [2026-03-13 15:36] 在后续“搜索并救援”类任务中，应采用分阶段闭环策略：1）先用 look_around 或系统化 fly_relative 路径建立基础覆盖；2）每次 observe 后必须更新内部状态（是否发现候选目标、位置置信度）；3）仅在目标已确认时才调用 rescue_person，且在 rescue_person 失败或 observe 多次失败时触发重规划（调整搜索模式或区域）；优先避免在缺乏目标信息的情况下直接调用救援技能。

- [2026-03-13 15:54] 对于搜索救援类任务，将策略从单一线性脚本改为‘覆盖规划驱动’：1）在任务开始时基于目标区域生成系统性的搜索路径（网格/条带/螺旋）；2）在每个节点执行 look_around + 多次 observe（必要时多高度）；3）按时间或观察次数设置中途评估点，若未发现目标则自动触发重规划（扩大范围、调整高度或改变网格密度）；4）在资源允许的前提下延长搜索直至达到覆盖率阈值或电量/时间阈值，而不是仅执行一轮粗略搜索后结束任务。

- [2026-03-13 15:54] 对于搜索救援类任务，将策略从单一线性脚本改为‘覆盖规划驱动’：1）在任务开始时基于目标区域生成系统性的搜索路径（网格/条带/螺旋）；2）在每个节点执行 look_around + 多次 observe（必要时多高度）；3）按时间或观察次数设置中途评估点，若未发现目标则自动触发重规划（扩大范围、调整高度或改变网格密度）；4）在资源允许的前提下延长搜索直至达到覆盖率阈值或电量/时间阈值，而不是仅执行一轮粗略搜索后结束任务。

- [2026-03-13 16:10] 对于搜索救援类任务，规划时应显式构建搜索区域模型并设计覆盖策略（如网格化/条带式航线），将 fly_to 用于到达主要航点，将 fly_relative 用于局部微调；在执行过程中，对 takeoff 等关键动作增加状态预检查与失败后的分支逻辑，一旦关键技能出现耗时为0.0s的失败应立即触发诊断或终止后续飞行动作，而不是继续按原计划执行。

- [2026-03-13 16:10] 对于搜索救援类任务，规划时应显式构建搜索区域模型并设计覆盖策略（如网格化/条带式航线），将 fly_to 用于到达主要航点，将 fly_relative 用于局部微调；在执行过程中，对 takeoff 等关键动作增加状态预检查与失败后的分支逻辑，一旦关键技能出现耗时为0.0s的失败应立即触发诊断或终止后续飞行动作，而不是继续按原计划执行。

- [2026-03-13 16:25] 在 UAV 状态正常且环境无明显障碍的巡检任务中，可优先采用线性、一次规划完成的巡检-返航流程：起点 observe → 关键点 look_around 与多段 fly_to 巡检 → 关键结果点 mark_location → return_to_launch，同时将返航时间纳入任务时间估计。

- [2026-03-13 16:25] 在 UAV 状态正常且环境无明显障碍的巡检任务中，可优先采用线性、一次规划完成的巡检-返航流程：起点 observe → 关键点 look_around 与多段 fly_to 巡检 → 关键结果点 mark_location → return_to_launch，同时将返航时间纳入任务时间估计。

- [2026-03-13 17:34] 在后续所有涉及飞行的任务规划中，增加统一前置步骤：1）读取 UAV 状态；2）若 status=error，则优先执行故障诊断/恢复子任务并终止当前高层任务规划；3）仅在状态恢复为正常后才允许规划 takeoff、navigate 等飞行技能，避免无效调用和重复失败。

- [2026-03-13 17:34] 在后续所有涉及飞行的任务规划中，增加统一前置步骤：1）读取 UAV 状态；2）若 status=error，则优先执行故障诊断/恢复子任务并终止当前高层任务规划；3）仅在状态恢复为正常后才允许规划 takeoff、navigate 等飞行技能，避免无效调用和重复失败。

- [2026-03-13 17:38] 在未来所有飞行相关任务中，规划阶段必须首先进行 UAV 健康状态检查（至少包含 status 字段）；若 status=error，则优先规划故障诊断/恢复或任务中止，而非直接调用 takeoff、导航或感知技能。

- [2026-03-13 17:38] 在未来所有飞行相关任务中，规划阶段必须首先进行 UAV 健康状态检查（至少包含 status 字段）；若 status=error，则优先规划故障诊断/恢复或任务中止，而非直接调用 takeoff、导航或感知技能。

- [2026-03-13 18:49] 在今后所有涉及飞行或感知的任务规划中，统一增加一个前置步骤：读取 UAV 状态并进行健康检查；若 status=error，则不进入飞行技能链，而是直接触发诊断/恢复流程或上报任务不可执行。这一检查应视为硬约束，而非可选优化。

- [2026-03-13 18:49] 在今后所有涉及飞行或感知的任务规划中，统一增加一个前置步骤：读取 UAV 状态并进行健康检查；若 status=error，则不进入飞行技能链，而是直接触发诊断/恢复流程或上报任务不可执行。这一检查应视为硬约束，而非可选优化。

- [2026-03-13 19:38] 对于状态为 idle、电量充足且未报告 error 的正常飞行状态，可将『change_altitude → fly_to → observe → look_around → hover』作为执行『到达目标点并观察周围环境』类任务的默认策略模板；规划时优先把时间预算分配给 observe 和 look_around 阶段，飞行阶段仅做必要的直线位移，不做过度复杂化的路径设计。

- [2026-03-13 19:38] 对于状态为 idle、电量充足且未报告 error 的正常飞行状态，可将『change_altitude → fly_to → observe → look_around → hover』作为执行『到达目标点并观察周围环境』类任务的默认策略模板；规划时优先把时间预算分配给 observe 和 look_around 阶段，飞行阶段仅做必要的直线位移，不做过度复杂化的路径设计。

- [2026-03-13 19:40] 在未来的任务规划中，遇到仅需获取 UAV 当前位置或基本状态的信息性任务时，优先直接调用 get_position 等只读状态技能；在 UAV.status ∈ {idle, flying} 且无 error 标记时，可省略额外冗余检查，以降低规划和执行开销。

- [2026-03-13 19:40] 在未来的任务规划中，遇到仅需获取 UAV 当前位置或基本状态的信息性任务时，优先直接调用 get_position 等只读状态技能；在 UAV.status ∈ {idle, flying} 且无 error 标记时，可省略额外冗余检查，以降低规划和执行开销。

- [2026-03-13 19:44] 在包含高层巡逻技能（如 patrol_area）的任务中：1）调用前必须进行前置条件自检；2）一旦巡逻技能出现耗时为0的即刻失败，应视为配置或状态问题，避免在同一任务中反复尝试，改为启动由 fly_relative + hover + look_around/observe 组成的规则化覆盖式备用巡逻；3）在任务层显式维护区域覆盖进度或检查点完成度，将“完成覆盖”作为主要终止条件，而不是仅依赖是否检测到异常。

- [2026-03-13 19:44] 在包含高层巡逻技能（如 patrol_area）的任务中：1）调用前必须进行前置条件自检；2）一旦巡逻技能出现耗时为0的即刻失败，应视为配置或状态问题，避免在同一任务中反复尝试，改为启动由 fly_relative + hover + look_around/observe 组成的规则化覆盖式备用巡逻；3）在任务层显式维护区域覆盖进度或检查点完成度，将“完成覆盖”作为主要终止条件，而不是仅依赖是否检测到异常。

- [2026-03-13 19:50] 在未来执行巡视类任务时，规划层应在任务开始和巡逻过程中周期性检查关键感知技能（look_around）和定位技能（get_position）的可用性：一旦出现长时失败或连续失败，应优先中止巡逻并进入安全模式（悬停、尝试恢复或返航），而不是继续执行飞行动作；同时在调用复合技能（如 patrol_area）前加入对 UAV 状态与关键子技能健康状况的前置校验，以提高整体任务成功率。

- [2026-03-13 19:50] 在未来执行巡视类任务时，规划层应在任务开始和巡逻过程中周期性检查关键感知技能（look_around）和定位技能（get_position）的可用性：一旦出现长时失败或连续失败，应优先中止巡逻并进入安全模式（悬停、尝试恢复或返航），而不是继续执行飞行动作；同时在调用复合技能（如 patrol_area）前加入对 UAV 状态与关键子技能健康状况的前置校验，以提高整体任务成功率。

- [2026-03-13 20:03] 在 UAV 状态正常、电量充足且环境假定为开阔无障碍时，优先采用轻量级的标准飞行流程（takeoff→高度调整→单段fly_to→observe→return_to_launch），并在时间预算中明确单独估算返航阶段时间；仅在存在障碍或状态异常线索时再引入复杂感知与重规划策略。

- [2026-03-13 20:03] 在 UAV 状态正常、电量充足且环境假定为开阔无障碍时，优先采用轻量级的标准飞行流程（takeoff→高度调整→单段fly_to→observe→return_to_launch），并在时间预算中明确单独估算返航阶段时间；仅在存在障碍或状态异常线索时再引入复杂感知与重规划策略。

- [2026-03-13 20:05] 在 UAV 状态正常且环境无明显障碍的前提下，规划类似“飞至指定坐标附近并搜索地面目标”的任务时，可以优先采用简单直线飞行 + 多次观测的策略：1）无需预设复杂路径或多段飞行，直接飞到目标坐标附近即可；2）在到达目标附近后，默认安排至少两次以上的观察（包括一次向下观测）来提高目标识别可靠性；3）在达到数米级位置精度后不必强行微调位置，将资源更多用于视觉搜索与确认。

- [2026-03-13 20:05] 在 UAV 状态正常且环境无明显障碍的前提下，规划类似“飞至指定坐标附近并搜索地面目标”的任务时，可以优先采用简单直线飞行 + 多次观测的策略：1）无需预设复杂路径或多段飞行，直接飞到目标坐标附近即可；2）在到达目标附近后，默认安排至少两次以上的观察（包括一次向下观测）来提高目标识别可靠性；3）在达到数米级位置精度后不必强行微调位置，将资源更多用于视觉搜索与确认。

- [2026-03-13 20:14] 在任务规划时引入“关键控制技能失败处理”策略：当 takeoff、land、mode 切换等技能连续返回 0.0s 耗时失败时，视为前置条件不满足而非暂时性错误，应立即触发状态自检（查询位置、高度、电量、状态），并基于自检结果调整后续步骤（例如：已接近地面则跳过 land，将任务视为完成；若状态异常则进入安全终止流程）。同时，在返航后增加基于高度和状态的条件式降落决策，而非无条件调用 land。

- [2026-03-13 20:14] 在任务规划时引入“关键控制技能失败处理”策略：当 takeoff、land、mode 切换等技能连续返回 0.0s 耗时失败时，视为前置条件不满足而非暂时性错误，应立即触发状态自检（查询位置、高度、电量、状态），并基于自检结果调整后续步骤（例如：已接近地面则跳过 land，将任务视为完成；若状态异常则进入安全终止流程）。同时，在返航后增加基于高度和状态的条件式降落决策，而非无条件调用 land。

- [2026-03-13 20:27] 在 UAV 状态正常（idle 且电量≥50%）且任务区域无已知障碍时，可以优先采用「takeoff 至约15米 → direct fly_to → observe → mark_location」的简洁技能链，作为地面明显目标搜索任务的默认策略模板，无需额外插入复杂规划与冗余状态检查，以提高执行效率。

- [2026-03-13 20:27] 在 UAV 状态正常（idle 且电量≥50%）且任务区域无已知障碍时，可以优先采用「takeoff 至约15米 → direct fly_to → observe → mark_location」的简洁技能链，作为地面明显目标搜索任务的默认策略模板，无需额外插入复杂规划与冗余状态检查，以提高执行效率。

- [2026-03-13 20:31] 在未来任务规划中，凡是使用 land 结束任务时，应在调用前加入状态/高度校验（可通过 get_status/get_position 等），并对 land 的瞬时失败（耗时≈0）做特殊处理：限制重试次数（例如最多 1 次），若连续失败则转为保持悬停并上报异常，而不是盲目重复 land；同时，将 return_to_launch 与 land 解耦看待，不再默认 RTH 后必然需要 land，而是依据当前飞行状态与任务要求决定是否执行着陆。

- [2026-03-13 20:31] 在未来任务规划中，凡是使用 land 结束任务时，应在调用前加入状态/高度校验（可通过 get_status/get_position 等），并对 land 的瞬时失败（耗时≈0）做特殊处理：限制重试次数（例如最多 1 次），若连续失败则转为保持悬停并上报异常，而不是盲目重复 land；同时，将 return_to_launch 与 land 解耦看待，不再默认 RTH 后必然需要 land，而是依据当前飞行状态与任务要求决定是否执行着陆。

- [2026-03-13 20:47] 在 UAV status=idle 且电量充足、无已知障碍信息的前提下，类似“起飞到固定高度→短距离定向飞行→观察→返航降落”的标准巡检任务，可优先采用当前这类简单线性技能链，不必引入额外的诊断或复杂规划步骤；根据经验，起飞和降落阶段是时间主耗，应在时间估算和优化时优先考虑对起降参数进行微调。

- [2026-03-13 20:47] 在 UAV status=idle 且电量充足、无已知障碍信息的前提下，类似“起飞到固定高度→短距离定向飞行→观察→返航降落”的标准巡检任务，可优先采用当前这类简单线性技能链，不必引入额外的诊断或复杂规划步骤；根据经验，起飞和降落阶段是时间主耗，应在时间估算和优化时优先考虑对起降参数进行微调。

- [2026-03-13 20:48] 在未来类似的静态地面目标搜索任务中，优先采用“起飞到任务高度 → 直接飞到目标坐标上方 → 至少两次向下观察 → 立即标记位置”的简化顺序策略；在 UAV 状态为 idle 且电量充足时，无需额外插入中间检查点或重规划逻辑，以减少不必要的决策复杂度。

- [2026-03-13 20:48] 在未来类似的静态地面目标搜索任务中，优先采用“起飞到任务高度 → 直接飞到目标坐标上方 → 至少两次向下观察 → 立即标记位置”的简化顺序策略；在 UAV 状态为 idle 且电量充足时，无需额外插入中间检查点或重规划逻辑，以减少不必要的决策复杂度。

- [2026-03-13 20:55] 在后续任务规划中，应显式强化收尾阶段的安全与鲁棒性：1) 将返航与降落视为独立子任务，规划中预留足够时间与电量，并在执行链中插入状态检查节点；2) 当任一关键收尾技能首次出现异常（长时耗或 FAIL）时，必须执行“诊断→决定是否重试→必要时切换备用策略”的固定流程，而不是简单重复同一技能；3) 对零耗时或极短耗时的 FAIL 做特别处理，将其视为前置条件未满足的信号，优先检查位姿和状态，而非盲目重试。

- [2026-03-13 20:55] 在后续任务规划中，应显式强化收尾阶段的安全与鲁棒性：1) 将返航与降落视为独立子任务，规划中预留足够时间与电量，并在执行链中插入状态检查节点；2) 当任一关键收尾技能首次出现异常（长时耗或 FAIL）时，必须执行“诊断→决定是否重试→必要时切换备用策略”的固定流程，而不是简单重复同一技能；3) 对零耗时或极短耗时的 FAIL 做特别处理，将其视为前置条件未满足的信号，优先检查位姿和状态，而非盲目重试。

- [2026-03-13 21:14] 在未来任务中，将返航阶段从‘附属步骤’提升为与主任务同等重要的阶段单独建模：规划时预留充足时间和电量、在触发 return_to_launch 前强制进行一次系统健康检查（状态 != error、剩余电量充足）、在历史上存在返航失败记录的环境或机体上优先考虑采用分段 fly_to 的手动返航方案作为备选。

- [2026-03-13 21:14] 在未来任务中，将返航阶段从‘附属步骤’提升为与主任务同等重要的阶段单独建模：规划时预留充足时间和电量、在触发 return_to_launch 前强制进行一次系统健康检查（状态 != error、剩余电量充足）、在历史上存在返航失败记录的环境或机体上优先考虑采用分段 fly_to 的手动返航方案作为备选。

- [2026-03-13 21:22] 在基于自然语言的路径规划任务中，对相对方向和距离指令（如“向东飞30米”）统一采用以起飞点为基准的世界坐标变换策略，并在调用 fly_to 前进行目标点合理性检查（距离、方向、是否超出飞行边界）；若飞行阶段长时间无障碍却失败，优先收集状态与位置用于诊断并考虑拆分为多段短距离飞行而非一次长距离飞行。

- [2026-03-13 21:22] 在基于自然语言的路径规划任务中，对相对方向和距离指令（如“向东飞30米”）统一采用以起飞点为基准的世界坐标变换策略，并在调用 fly_to 前进行目标点合理性检查（距离、方向、是否超出飞行边界）；若飞行阶段长时间无障碍却失败，优先收集状态与位置用于诊断并考虑拆分为多段短距离飞行而非一次长距离飞行。

- [2026-03-13 21:24] 在涉及返航的任务中，当 return_to_launch 首次失败且 UAV 状态非 error、仍在空中时，优先执行顺序：1) 调用 get_position 获取最新位置信息；2) 基于位置信息重新尝试 return_to_launch；3) 若返航再次失败，则考虑使用记录的起飞点与当前位置通过 fly_relative 或路径规划构造备选返航方案。

- [2026-03-13 21:24] 在涉及返航的任务中，当 return_to_launch 首次失败且 UAV 状态非 error、仍在空中时，优先执行顺序：1) 调用 get_position 获取最新位置信息；2) 基于位置信息重新尝试 return_to_launch；3) 若返航再次失败，则考虑使用记录的起飞点与当前位置通过 fly_relative 或路径规划构造备选返航方案。

- [2026-03-13 21:32] 在包含明确目标坐标与目标物体的任务中，规划层应采用“先精确导航，后结构化搜索，最后给出明确结论”的三阶段策略：1）优先使用 fly_to 到达目标坐标，失败时结合 get_position 进行1–2次容错重试；2）若仍无法精确到达或在目标点附近未发现目标，则在限定半径和时间内使用规则化搜索模式（基于 fly_relative + observe/look_around），并记录搜索覆盖范围；3）在搜索结束后根据搜索范围与感知结果，要么报告目标位置和周围物体，要么明确报告在给定范围内未发现目标，而不是在没有结论的情况下结束任务。

- [2026-03-13 21:32] 在包含明确目标坐标与目标物体的任务中，规划层应采用“先精确导航，后结构化搜索，最后给出明确结论”的三阶段策略：1）优先使用 fly_to 到达目标坐标，失败时结合 get_position 进行1–2次容错重试；2）若仍无法精确到达或在目标点附近未发现目标，则在限定半径和时间内使用规则化搜索模式（基于 fly_relative + observe/look_around），并记录搜索覆盖范围；3）在搜索结束后根据搜索范围与感知结果，要么报告目标位置和周围物体，要么明确报告在给定范围内未发现目标，而不是在没有结论的情况下结束任务。

- [2026-03-13 21:34] 在所有涉及飞行的任务规划模板中，加入统一的前置健康检查策略：1）先调用状态/健康类技能检查 UAV status 和故障标志；2）若 status!=正常，则优先执行故障诊断或直接终止任务并上报，而不是尝试起飞；3）若起飞首次失败，除非错误类型明确可通过一次重试解决，否则不进行盲目多次 takeoff 重试。

- [2026-03-13 21:34] 在所有涉及飞行的任务规划模板中，加入统一的前置健康检查策略：1）先调用状态/健康类技能检查 UAV status 和故障标志；2）若 status!=正常，则优先执行故障诊断或直接终止任务并上报，而不是尝试起飞；3）若起飞首次失败，除非错误类型明确可通过一次重试解决，否则不进行盲目多次 takeoff 重试。

- [2026-03-13 21:35] 在今后涉及飞行任务的规划中，增加一个前置步骤：读取 UAV 状态并进行健康检查。如果 status=error，则不直接调用任何飞行或位置相关技能，而是返回上层需要执行故障诊断/恢复的提示；仅在状态正常时才继续执行 takeoff 和后续路径规划。

- [2026-03-13 21:35] 在今后涉及飞行任务的规划中，增加一个前置步骤：读取 UAV 状态并进行健康检查。如果 status=error，则不直接调用任何飞行或位置相关技能，而是返回上层需要执行故障诊断/恢复的提示；仅在状态正常时才继续执行 takeoff 和后续路径规划。

- [2026-03-14 16:12] 在后续任务规划中，避免将 return_to_launch 作为唯一的任务收尾手段：为其设置最多 1–2 次重试和固定超时时间；一旦出现连续失败，应立刻触发 get_position 等状态检查，并优先考虑简单可控的替代方案（如手动规划回程或直接 land）。在无障碍的简单任务中，可将“直接降落”作为默认兜底策略，以提高整体任务成功率和安全性。

- [2026-03-14 16:12] 在后续任务规划中，避免将 return_to_launch 作为唯一的任务收尾手段：为其设置最多 1–2 次重试和固定超时时间；一旦出现连续失败，应立刻触发 get_position 等状态检查，并优先考虑简单可控的替代方案（如手动规划回程或直接 land）。在无障碍的简单任务中，可将“直接降落”作为默认兜底策略，以提高整体任务成功率和安全性。

- [2026-03-14 16:14] 在环境正常、无障碍且 UAV 状态非 error 时，可以优先采用简单线性任务链（起飞→飞到目标区域→多次观察→标记位置），并默认进行至少两次观察以提高识别可靠性；仅在状态异常或环境复杂时再引入重规划和额外安全检查。

- [2026-03-14 16:14] 在环境正常、无障碍且 UAV 状态非 error 时，可以优先采用简单线性任务链（起飞→飞到目标区域→多次观察→标记位置），并默认进行至少两次观察以提高识别可靠性；仅在状态异常或环境复杂时再引入重规划和额外安全检查。

- [2026-03-14 16:17] 在任务结束阶段，将 land 设为默认且高优先级的安全终止手段：规划时可优先使用 land 完成任务收尾，仅在需要回到特定起飞点且环境明确安全时再使用 return_to_launch，并为 return_to_launch 设置明确超时与自动切换到 land 的回退机制。

- [2026-03-14 16:17] 在任务结束阶段，将 land 设为默认且高优先级的安全终止手段：规划时可优先使用 land 完成任务收尾，仅在需要回到特定起飞点且环境明确安全时再使用 return_to_launch，并为 return_to_launch 设置明确超时与自动切换到 land 的回退机制。

- [2026-03-14 16:38] 在后续任务规划中，对返航阶段采用冗余策略：优先尝试 return_to_launch，但必须设置明确的时间上限和状态检查；一旦该技能接近超时或返回 FAIL，立即切换为基于 fly_to+land 的手动返航流程。同时，在任务收尾逻辑中增加一条规则：只要末尾飞行技能失败且 UAV status=error，即刻在后续任务前插入状态诊断/恢复步骤，而不是继续常规飞行。

- [2026-03-14 16:38] 在后续任务规划中，对返航阶段采用冗余策略：优先尝试 return_to_launch，但必须设置明确的时间上限和状态检查；一旦该技能接近超时或返回 FAIL，立即切换为基于 fly_to+land 的手动返航流程。同时，在任务收尾逻辑中增加一条规则：只要末尾飞行技能失败且 UAV status=error，即刻在后续任务前插入状态诊断/恢复步骤，而不是继续常规飞行。

- [2026-03-14 16:40] 在后续任务规划中，采用“单次起飞 + 多点巡航”的默认模式：仅在任务开始时调用一次 takeoff，之后使用 fly_to+hover+observe 在各目标点之间移动；在任何再次调用 takeoff 之前，必须先检查当前高度与状态，若 UAV 已在空中且 status=idle，则跳过多余的 takeoff 调用，并且在任务评估时以核心目标完成情况为主，避免因可预期的冗余技能失败而将整体任务判为失败。

- [2026-03-14 16:40] 在后续任务规划中，采用“单次起飞 + 多点巡航”的默认模式：仅在任务开始时调用一次 takeoff，之后使用 fly_to+hover+observe 在各目标点之间移动；在任何再次调用 takeoff 之前，必须先检查当前高度与状态，若 UAV 已在空中且 status=idle，则跳过多余的 takeoff 调用，并且在任务评估时以核心目标完成情况为主，避免因可预期的冗余技能失败而将整体任务判为失败。

- [2026-03-18 13:27] 在后续任务规划中，将基础状态技能（尤其 get_position）设置为飞行任务的必需前置检查：若首次 get_position 失败，则暂停后续 takeoff 和移动指令，转入“状态诊断/恢复”子流程（检查 UAV status、错误标志、通信情况），仅在诊断通过后才重新尝试飞行；同时为 takeoff 等关键技能引入失败重试上限和失败后的安全退出策略。

- [2026-03-18 13:27] 在后续任务规划中，将基础状态技能（尤其 get_position）设置为飞行任务的必需前置检查：若首次 get_position 失败，则暂停后续 takeoff 和移动指令，转入“状态诊断/恢复”子流程（检查 UAV status、错误标志、通信情况），仅在诊断通过后才重新尝试飞行；同时为 takeoff 等关键技能引入失败重试上限和失败后的安全退出策略。

- [2026-03-18 15:13] 在后续任务规划中，对于以状态为目标的任务（如起飞/降落/上锁等），应显式设计两种路径：1）若当前状态已满足目标，则走“确认+直接完成”的快捷路径并清晰记录；2）若未满足，则规划具体技能链。否则会出现名义上的成功但缺乏可学习经验的记录。

- [2026-03-18 15:13] 在后续任务规划中，对于以状态为目标的任务（如起飞/降落/上锁等），应显式设计两种路径：1）若当前状态已满足目标，则走“确认+直接完成”的快捷路径并清晰记录；2）若未满足，则规划具体技能链。否则会出现名义上的成功但缺乏可学习经验的记录。

- [2026-03-18 15:14] 在未来规划类似起飞任务时，如果初始状态已经为 airborne，应显式采用“目标已满足，跳过技能执行”的快速完成策略，并在任务记录中标记为状态满足型成功，而不是技能执行型成功，以便后续不会错误地从这类任务中推断出关于起飞技能性能的经验。

- [2026-03-18 15:14] 在未来规划类似起飞任务时，如果初始状态已经为 airborne，应显式采用“目标已满足，跳过技能执行”的快速完成策略，并在任务记录中标记为状态满足型成功，而不是技能执行型成功，以便后续不会错误地从这类任务中推断出关于起飞技能性能的经验。

- [2026-03-18 22:46] 在所有涉及飞行或状态查询的任务中，引入统一的前置检查步骤：若 UAV.status == 'error'，则禁止调用常规飞行/感知技能，转而触发诊断/恢复流程或直接标记任务不可执行。对于同一技能出现“先长耗时执行、后立即 0s 失败”的模式时，优先进行系统级状态检查，而不是简单重试技能。

- [2026-03-18 22:46] 在所有涉及飞行或状态查询的任务中，引入统一的前置检查步骤：若 UAV.status == 'error'，则禁止调用常规飞行/感知技能，转而触发诊断/恢复流程或直接标记任务不可执行。对于同一技能出现“先长耗时执行、后立即 0s 失败”的模式时，优先进行系统级状态检查，而不是简单重试技能。

- [2026-03-28 00:04] 多点巡检任务优先采用 fly_to → observe → report 的逐点闭环模式，不建议批量飞行后统一感知；起飞时间应在任务总时长预算中单独计入约25s。

- [2026-03-28 00:04] 多点巡检任务优先采用 fly_to → observe → report 的逐点闭环模式，不建议批量飞行后统一感知；起飞时间应在任务总时长预算中单独计入约25s。
